// ============================================================
// 부품 — 메뉴 카드
// ------------------------------------------------------------
// 08단원 개념04에서 씁니다.
// 03단원 개념05의 MenuCard 를 그대로 옮겨 온 것입니다.
// note 를 안 넘기면 기본값이 쓰이는 것도 그대로입니다(03단원 개념03).
// ============================================================

export default function MenuCard({ name, price, note = "설명 없음" }) {
  return (
    <div className="output">
      <strong>{name}</strong> — {price}원
      <br />
      <small>{note}</small>
    </div>
  );
}
