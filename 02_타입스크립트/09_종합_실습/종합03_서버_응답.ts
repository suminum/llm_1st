// ============================================================
// 09단원 · 종합 03 — 서버 응답 다루기
// ------------------------------------------------------------
// 실행: node 종합03_서버_응답.ts
// 채점: npm run grade
// 검사: npm run check
// ------------------------------------------------------------
// 사용 단원: 04(서버 데이터) · 05(null·판별 유니온) · 08(unknown 확인)
// ============================================================
//
// 서버가 보낸 JSON 을 안전하게 다루는 흐름을 만듭니다.
// 이 자료에서 가장 실무에 가까운 실습입니다.
//
// ★ 규칙 — any · ! · as 를 쓰지 않습니다.
//   특히 as 를 쓰고 싶어지는 자리가 나올 텐데, 쓰지 말고 확인하세요.

// ── 주어진 데이터 (고치지 마세요) ──
// 서버가 보냈다고 치는 문자열 넷입니다. 하나만 제대로 왔습니다.

const okText = '{"name":"라떼","price":4500}';
const missingText = '{"name":"라떼"}'; //  price 가 없습니다
const wrongText = '{"name":123,"price":4500}'; //  name 이 숫자입니다
const brokenText = "<html>502 Bad Gateway</html>"; //  JSON 이 아예 아닙니다

console.log("===== 서버 응답 =====");


// ───── 문제 1 ───── 모양을 적으면 생기는 일 (04단원 개념04)
// Menu 타입(name: string, price: number)을 만들고
// okText 와 missingText 를 각각 JSON.parse 해서 : Menu 로 받으세요.
// 그다음 "이름 가격" 을 출력하세요.
//
// ★ missingText 쪽에서 이상한 값이 나올 것입니다. 그게 이 문제의 요점입니다.
//   검사는 조용한데 실행하면 틀린 값이 나옵니다.
//
// 기대 출력:
// 라떼 4500
// 라떼 undefined

// TODO: type Menu 를 만들고 둘을 받아 출력하세요


// ───── 문제 2 ───── 확인하고 쓰기 (08단원 개념01 섹션3)
// 문자열을 받아 '확인을 통과한 Menu' 또는 null 을 돌려주는 parseMenu 를 만드세요.
//   · JSON.parse 를 try / catch 로 감쌉니다. 실패하면 null (brokenText 가 이 경우입니다)
//   · JSON.parse 결과를 unknown 으로 받습니다 (any 로 받으면 확인이 무의미합니다)
//   · 객체인지 · null 이 아닌지 · 이름이 있는지 · 종류가 맞는지 확인합니다
//   · 하나라도 어긋나면 null
//
// ★ try 를 빠뜨리면 brokenText 에서 프로그램이 통째로 죽습니다.
//   타입 검사는 이걸 못 잡습니다. 직접 빼 보고 확인하세요.
//
// 기대 출력:
// { name: '라떼', price: 4500 }
// null
// null
// null

// TODO: parseMenu 를 만들고 okText · missingText · wrongText · brokenText 를 각각 넘겨 출력하세요


// ───── 문제 3 ───── 없을 수 있는 값 (05단원 개념03)
// 아래 가게 정보를 다룹니다. 타입은 이렇게 적으세요.
//
//   Shop = { name: string; owner: { name: string; phone: string | null } | null }
//
// ① shop1 의 주인 전화번호를 출력하세요. 없으면 "번호 없음"
// ② shop2 의 주인 이름을 출력하세요. 없으면 "주인 없음"
//
// ★ ! 를 쓰면 안 됩니다. ?. 와 ?? 로 하세요.
//
// 기대 출력:
// 번호 없음
// 주인 없음

// TODO: type Shop 을 만들고 아래 두 값에 붙인 뒤 출력하세요
const rawShop1 = { name: "봄날카페", owner: { name: "홍길동", phone: null } };
const rawShop2 = { name: "무인카페", owner: null };


// ───── 문제 4 ───── 화면 상태 (05단원 개념04)
// 응답 처리 결과를 판별 유니온으로 표현하세요.
//
//   LoadState =
//     | { status: "로딩중" }
//     | { status: "성공"; menu: Menu }
//     | { status: "실패"; message: string }
//
// 상태를 받아 문구를 돌려주는 render 를 switch 로 만드세요.
//   로딩중 → "불러오는 중..."
//   성공   → "라떼 4500원"  (이름 가격원)
//   실패   → "오류: 형식이 다릅니다"
//
// 기대 출력:
// 불러오는 중...
// 라떼 4500원
// 오류: 형식이 다릅니다

// TODO: LoadState 와 render 를 만들고 세 상태를 각각 넘겨 출력하세요


// ───── 문제 5 ───── 조립 (문제 2 + 문제 4)
// 문자열 하나를 받아 LoadState 를 돌려주는 load 를 만드세요.
//   · parseMenu 가 null 을 주면 → { status: "실패", message: "형식이 다릅니다" }
//   · 아니면 → { status: "성공", menu: ... }
//
// 기대 출력:
// 라떼 4500원
// 오류: 형식이 다릅니다

// TODO: load 를 만들고 okText · missingText 를 각각 넣어 render 결과를 출력하세요


// ───── 문제 6 ───── [도전] 처리 결과표
// 세 응답을 전부 처리해서 표로 출력하세요.
//   ① "----- 처리 결과 -----"
//   ② "N번: 문구"  (성공이면 render 결과, 실패면 message 그대로)
//   ③ "성공 1건 / 실패 2건"
//
// 힌트: 성공·실패는 load 결과의 status 로 셉니다.
//       ★ render 결과에 replace 를 쓰지 마세요. 표딱지가 있으면 표딱지로 갈라야 합니다.
//
// 기대 출력:
// ----- 처리 결과 -----
// 1번: 라떼 4500원
// 2번: 형식이 다릅니다
// 3번: 형식이 다릅니다
// 성공 1건 / 실패 2건

// TODO: 세 응답을 처리해 표를 출력하세요
