// ============================================================
// 01단원 · 개념 02 — 실행과 검사는 따로다  ★ 이 단원에서 제일 중요한 파일
// ------------------------------------------------------------
// 실행: node 개념02_실행과_검사는_따로다.ts
// 검사: npm run typecheck
// ============================================================
//
// 이 파일을 안 읽으면 반드시 헤맵니다. 여기만은 꼭 읽으세요.
//
// JS자료에서는 명령이 하나였습니다.
//
//     node 파일.js       ← 실행하면 결과도 보이고 에러도 보였다
//
// 타입스크립트는 명령이 둘입니다. 그리고 둘이 하는 일이 완전히 다릅니다.
//
//     node 파일.ts             ← 실행만 한다. 타입은 안 본다.
//     npx tsc --noEmit         ← 검사만 한다. 실행은 안 한다.
//
// 이걸 모르면 "타입을 틀리게 썼는데 왜 아무 말도 없지?" 하고 한참 헤맵니다.


// ── 섹션 1: node 는 타입을 '지우고' 실행한다 ──

// 여러분이 이렇게 쓰면
const price: number = 4000;
const menus: string[] = ["아메리카노", "라떼"];
//         └───┬───┘
//         "문자열이 들어 있는 배열" 이라는 뜻입니다. 02단원에서 배웁니다.
//         지금은 '타입 표기가 하나 더 있다' 정도로만 보세요.

// node 는 실행하기 전에 타입 표기를 지웁니다. 이렇게 됩니다.
//
//     const price = 4000;
//     const menus = ["아메리카노", "라떼"];
//
// : number 와 : string[] 은 흔적도 없이 사라집니다.

console.log(price, menus);
// 출력: 4000 [ '아메리카노', '라떼' ]

// 여기서 중요한 것은 '지운다' 지 '검사하고 지운다' 가 아니라는 점입니다.
// 맞는지 틀리는지 보지도 않고 그냥 지웁니다.
//
//     그래서 타입이 틀려도 화면은 그냥 돌아갑니다.
//     빨간 화면도, 콘솔 경고도, 아무것도 안 뜹니다.
//
// JS자료(01~13단원)에서는 실수하면 화면이 깨지거나 콘솔에 빨간 줄이 나왔습니다.
// 그것에 익숙해져 있으면 여기서 반드시 걸려 넘어집니다.

// ✏️ 직접 해보기 1 — 위 price 를 const price: string = 4000; 으로 고치고
//    node 로 실행해 보세요. 화면이 어떻게 되는지 확인한 뒤 되돌리세요.


// ── 섹션 2: 왜 그렇게 만들어 놨나 ──
// (✏️ 없음: 왜 그렇게 만들었는지를 읽는 섹션입니다. 해 볼 것이 없습니다)

// 타입 검사는 프로젝트 전체를 한 번 훑어야 합니다. 파일이 많으면 시간이 걸립니다.
// 실행할 때마다 그걸 기다리면 개발이 느려집니다.
//
// 그래서 역할을 나눴습니다.
//
//     node  : 빨리 돌려서 결과를 보여 주는 일
//     tsc   : 꼼꼼히 검사하는 일
//
// 07단원에서 만나는 Vite 도 똑같습니다. 타입을 검사하지 않고 지웁니다.
// 도구가 달라져도 이 구조는 그대로입니다.


// ── 섹션 3: 직접 확인해 보기 ★ ──

// 이 단원 폴더 안에 틀린예제/ 라는 폴더가 있습니다.
// 그 안의 타입이_틀린_파일.ts 는 타입이 세 군데나 틀린 채로 두었습니다.
//
// TS자료 폴더에서 아래 두 줄을 순서대로 쳐 보세요.
//
//   1) node 01_타입스크립트_시작하기/틀린예제/타입이_틀린_파일.ts
//   2) npx tsc --noEmit -p 01_타입스크립트_시작하기/틀린예제
//
// 1번 결과 — 끝까지 잘 돌아갑니다.
//
//     가격: 4000
//     정말 숫자인가: string
//     메뉴 가격: undefined
//     100보다 큰 수 + 1 = NaN
//     ---
//     이 파일은 끝까지 다 돌았습니다. 타입이 세 군데나 틀렸는데도요.
//
// 2번 결과 — 같은 파일인데 에러가 세 개 나옵니다.
//
//     ...(23,7): error TS2322: Type 'string' is not assignable to type 'number'.
//     ...(41,28): error TS2551: Property 'pirce' does not exist on ... Did you mean 'price'?
//     ...(53,32): error TS18048: 'big' is possibly 'undefined'.
//
// 1번에서 눈여겨볼 줄은 이것입니다.
//
//     정말 숫자인가: string
//
// : number 라고 적어 놓았는데 실제로 들어 있는 것은 문자열입니다.
// node 는 그 표기를 지우고 실행했기 때문에 아무 일도 일어나지 않았습니다.
// 화면의 "가격: 4000" 은 숫자 4000 과 눈으로 구별이 안 됩니다.

// ✏️ 직접 해보기 2 — 위 두 명령을 실제로 쳐 보고,
//    2번에서 나온 에러 세 개가 각각 몇 번째 줄인지 파일을 열어 확인해 보세요.


// ── 섹션 4: 그럼 타입 에러는 어디서 보나 — 두 곳뿐이다 ──

// [곳 1] VS Code 의 빨간 물결 밑줄
//
//   틀린 자리에 빨간 물결이 생깁니다. 저장하지 않아도 바로 나옵니다.
//   그 위에 마우스를 올리면 메시지가 뜹니다.
//   메모장이나 다른 편집기로 열면 이 밑줄이 안 보입니다.
//   이 자료는 VS Code 로 여는 것을 전제합니다.
//
// [곳 2] 터미널에서 npx tsc --noEmit
//
//   TS자료 폴더에서 칩니다. 프로젝트 전체를 한 번에 검사합니다.

// tsc 는 TypeScript Compiler 의 줄임말이고,
// --noEmit 은 "파일은 만들지 말고 검사만 해라" 라는 뜻입니다.
// 실행은 node 가 하니까 검사만 시키는 것입니다.
//
// ── 짧은 이름 ──
//
// 이 긴 명령을 매번 치기 번거로우니 짧은 이름으로 등록해 두었습니다.
//
//     npm run typecheck      ←  npx tsc --noEmit 과 완전히 같습니다
//
// 앞으로 파일 머리글에는 짧은 쪽이 적혀 있습니다.
// 어느 쪽을 쳐도 결과는 같으니 편한 것을 쓰세요.
// (등록해 둔 곳은 package.json 의 scripts 입니다. 08단원에서 다시 봅니다)

// ✏️ 직접 해보기 3 — 지금 TS자료 폴더에서 npx tsc --noEmit 을 쳐 보세요.
//    무엇이 나오는지(또는 안 나오는지) 확인하면 됩니다.


// ── 섹션 5: 조용하면 통과다 ──

// 문제가 없으면 tsc 는 아무것도 안 찍습니다.
//
//     > npx tsc --noEmit
//     >
//
// "성공" 이나 "OK" 같은 글자는 안 나옵니다.
// 처음 보면 "명령이 안 먹었나?" 싶지만 정상입니다.
//
//     아무것도 안 나오는 것이 통과입니다.
//
// 이 자료에서는 이것을 '조용하다' 고 부르겠습니다.

// ✏️ 직접 해보기 4 — 이 파일 아무 데나 const x: number = "글자"; 를 한 줄 넣고
//    npx tsc --noEmit 을 쳐 보세요. 조용하지 않게 되는 것을 확인한 뒤 지우세요.


// ── 섹션 6: 확장자가 바뀐다 ──

// 지금까지         타입을 쓰려면
//   .js       →      .ts
//   .jsx      →      .tsx      (07단원 React 에서)
//
// 이 자료의 01~06단원은 전부 .ts 입니다.
// 실행은 JS자료와 똑같이 node 파일이름.ts 입니다. 빌드 같은 것은 없습니다.
//
// 참고: node 가 .ts 를 바로 실행하는 것은 Node 22 이후 기능입니다.
//       예전 자료나 인터넷 글에서 ts-node 니 tsx 니 하는 도구를 설치하라고
//       하는 것을 볼 수 있는데, 지금은 필요 없습니다.
//       터미널에서 node --version 을 쳐서 v22 이상이면 됩니다.

// ✏️ 직접 해보기 5 — node --version 을 쳐서 버전을 확인해 보세요.


// ── 섹션 7: 자주 하는 실수 ──

// [실수 1] node 로 돌려 보고 "타입 다 맞았네" 라고 넘어가기
//   node 는 타입을 안 봅니다. 검사는 npx tsc --noEmit 로만 됩니다.
//   이 자료에서 제일 많이 나오는 실수입니다.

// [실수 2] 빨간 밑줄이 있는데 화면이 잘 나오니까 무시하기
//   지금은 넘어가도 되지만, 07단원에서 화면이 안 나오는 진짜 원인이 됩니다.
//   빨간 밑줄은 "지금 당장은 안 터지지만 곧 터진다" 는 뜻입니다.

// [실수 3] tsc 가 조용한 것을 "명령이 실패했다" 고 착각하기
//   조용한 것이 통과입니다. 섹션 5를 다시 보세요.

// [실수 4] 타입 에러가 났는데 파일을 저장 안 하고 다시 검사하기
//   VS Code 의 빨간 밑줄은 저장 없이도 갱신되지만,
//   npx tsc --noEmit 은 '저장된 파일' 을 읽습니다. 고쳤으면 저장부터 하세요.


// ── 정리 ──

// 1. 명령이 두 개다. node 는 실행만, npx tsc --noEmit 은 검사만 한다.
// 2. node 는 타입을 검사하지 않고 지운다. 그래서 타입이 틀려도 그냥 돌아간다.
// 3. 타입 에러를 보는 곳은 둘뿐이다 — VS Code 빨간 밑줄, npx tsc --noEmit.
// 4. tsc 가 아무 말도 안 하면 통과다. '성공' 같은 글자는 안 나온다.
// 5. 확장자는 .ts (React 는 .tsx). 실행법은 node 파일.ts 로 JS자료와 같다.
// 6. 07단원의 Vite 도 똑같이 타입을 지운다. 도구가 바뀌어도 구조는 같다.


// ============================================================
// 직접 해보기 정답
// ============================================================
//
// 1) 화면은 아무 일 없이 그대로 돌아갑니다. 출력도 4000 그대로입니다.
//    : string 이라고 적어 놓고 숫자 4000 을 넣었는데도 그렇습니다.
//    VS Code 에는 빨간 밑줄이 생기고, npx tsc --noEmit 에는 이렇게 나옵니다.
//    error TS2322: Type 'number' is not assignable to type 'string'.
//    재현:
//    const price: string = 4000;
//    → 화면과 검사가 따로 논다는 것을 직접 본 것입니다.
//
// 2) 23줄 · 41줄 · 53줄 입니다.
//    파일을 열어 보면 각각 const price: number = "4000" /
//    cafe.pirce / big + 1 자리입니다.
//    괄호 안의 두 번째 숫자(7, 28, 32)는 그 줄의 몇 번째 글자인지입니다.
//
// 3) 아무것도 안 나옵니다. 이 자료의 개념·정답 파일은 전부 검사를 통과한 상태입니다.
//    두 가지가 이 검사에서 빠져 있습니다.
//      · 틀린예제/ 폴더 — 일부러 틀린 파일이니까요
//      · 연습문제.ts — 안 푼 상태에서 걸리는 게 정상이라 npm run check 로 따로 봅니다
//    그래서 연습문제를 아직 안 풀었어도 이 명령은 조용합니다.
//
// 4) 이렇게 나옵니다.
//    01_타입스크립트_시작하기/개념02_실행과_검사는_따로다.ts(줄,열):
//    error TS2322: Type 'string' is not assignable to type 'number'.
//    재현:
//    const x: number = "글자";
//    지우고 다시 치면 다시 조용해집니다.
//
// 5) v22 이상이면 됩니다.
//    낮은 버전이면 node 파일.ts 가 실행 자체를 거부합니다
//    (".ts 확장자를 모른다" 는 뜻의 에러가 납니다. 문구는 버전마다 조금 다릅니다).
//    그때는 nodejs.org 에서 새 버전을 받아 까세요.
