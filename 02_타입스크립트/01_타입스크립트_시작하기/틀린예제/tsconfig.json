{
  // 이 폴더는 '일부러 틀린 파일' 이 들어 있는 곳입니다.
  // 위쪽 tsconfig.json 은 이 폴더를 검사에서 빼 놓았습니다.
  // 그래서 이 폴더만 따로 검사하려면 이렇게 칩니다.
  //
  //     npx tsc --noEmit -p 01_타입스크립트_시작하기/틀린예제
  //
  "extends": "../../tsconfig.json",
  "include": ["*.ts"],
  "exclude": []
}
