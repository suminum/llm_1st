// ============================================================
// 02단원 · 개념 02 — 추론: 대부분은 안 적어도 된다
// ------------------------------------------------------------
// 실행: node 개념02_추론.ts
// 검사: npm run typecheck
// ============================================================
//
// 지금까지 타입을 꼬박꼬박 적었습니다. 손에 익히려고 그런 것입니다.
// 그런데 실무 코드를 보면 타입이 생각보다 훨씬 적게 적혀 있습니다.
//
// 타입스크립트가 값을 보고 알아서 알아내기 때문입니다.
// 이것을 '추론' 이라고 합니다.


// ── 섹션 1: 값이 옆에 있으면 안 적어도 된다 ──

// 이렇게 적어도
const price1: number = 4000;

// 이렇게 적어도
const price2 = 4000;

// 둘은 완전히 같습니다. 아래쪽도 number 입니다.
console.log(price1, price2);
// 출력: 4000 4000

// 4000 을 보고 "이건 숫자군" 하고 알아냈기 때문입니다.
// 사람이 봐도 뻔한 것은 타입스크립트도 압니다.

// 안 적었어도 검사는 똑같이 합니다.
//
// 에러: TS2339 Property 'toUpperCase' does not exist on type '4000'.
// console.log(price2.toUpperCase());
//
// 실수: 타입을 안 적었다고 검사가 느슨해지는 게 아닙니다.
//       적어 둔 것과 똑같이 막습니다.

// ✏️ 직접 해보기 1 — const city = "서울"; 을 만들고
//    city.toFixed(2) 를 써 보세요. 무슨 에러가 나는지 확인 후 지우세요.


// ── 섹션 2: 추론된 타입을 눈으로 보는 두 가지 방법 ──

const menu = "아메리카노";

// [방법 1] VS Code 에서 이름 위에 마우스를 올린다
//   const menu: "아메리카노"  라고 뜹니다. 가장 편한 방법입니다.
//
// [방법 2] 일부러 틀리게 담아 보고 에러 메시지를 읽는다
//   이 방법은 편집기 없이도 되고, 나중에 복잡한 타입을 볼 때 유용합니다.
//
// 에러: TS2322 Type '"아메리카노"' is not assignable to type 'null'.
// const peek: null = menu;
//
// 실수: null 자리에는 아무것도 못 들어가니 반드시 에러가 납니다.
//       그 메시지의 왼쪽에 '진짜 타입' 이 그대로 적혀 나옵니다.
//       궁금할 때 쓰는 요령이니 알아 두세요. 확인 후 지웁니다.

console.log(menu);
// 출력: 아메리카노

// ✏️ 직접 해보기 2 — const stock = [1, 2, 3]; 의 타입이 무엇인지
//    위 두 방법 중 하나로 확인해 보세요.


// ── 섹션 3: const 와 let 은 추론 결과가 다르다 ──

// 같은 값을 넣었는데 타입이 다릅니다.
const fixed = 5; //  타입: 5
let changing = 5; //  타입: number

console.log(fixed, changing);
// 출력: 5 5

// const 는 '5' 라는 값 자체가 타입이 됩니다. 리터럴 타입이라고 부릅니다.
// 왜냐하면 const 는 다시 대입할 수 없으니, 영원히 5 일 것이 확실하기 때문입니다.
//
// let 은 나중에 바뀔 수 있으니 넉넉하게 number 로 잡습니다.

// let 은 같은 종류 안에서는 자유롭게 바뀝니다.
changing = 10;
console.log(changing);
// 출력: 10

// 하지만 종류를 넘어가면 걸립니다.
//
// 에러: TS2322 Type 'string' is not assignable to type 'number'.
// changing = "열";
//
// 실수: 처음에 5 를 넣은 순간 "이 변수는 숫자를 담는 자리" 로 정해졌습니다.
//       JS 에서는 아무 값이나 다시 넣을 수 있던 것이 여기서는 막힙니다.

// 리터럴 타입이 왜 쓸모가 있는지는 05단원에서 나옵니다.
// 지금은 "const 는 값 자체가 타입이 된다" 정도만 기억하세요.

// ✏️ 직접 해보기 3 — let city = "서울"; 을 만든 뒤
//    city = "부산"; 과 city = 3; 을 각각 해 보세요. 어느 쪽이 걸리나요?


// ── 섹션 4: 객체와 배열도 알아서 알아낸다 ──

const order = {
  menu: "라떼",
  count: 2,
  isHot: true,
};

// order 의 타입은 이렇게 추론됩니다.
//     { menu: string; count: number; isHot: boolean; }
// 하나하나 적을 필요가 없습니다.

console.log(order.menu, order.count);
// 출력: 라떼 2

// 없는 이름은 여전히 막힙니다.
//
// 에러: TS2339 Property 'size' does not exist on type '{ menu: string; count: number; isHot: boolean; }'.
// console.log(order.size);
//
// 실수: 적어 두지 않았는데도 "이 객체에 뭐가 있는지" 를 정확히 알고 있습니다.

// 배열도 같습니다.
const prices = [4000, 4500, 5000];
console.log(prices.length);
// 출력: 3

// prices 의 타입은 number[] 입니다.
//
// 에러: TS2345 Argument of type 'string' is not assignable to parameter of type 'number'.
// prices.push("육천원");
//
// 실수: 처음에 숫자만 넣었으니 숫자 배열로 정해졌습니다.

// 참고 — 객체 안의 객체, 배열 안의 객체도 전부 알아냅니다.
const shop = {
  name: "봄날카페",
  menus: [
    { name: "아메리카노", price: 4000 },
    { name: "라떼", price: 4500 },
  ],
};
console.log(shop.menus[0].price);
// 출력: 4000

// shop.menus[0].price 까지 자동완성이 됩니다. 아무것도 안 적었는데도요.
// 이게 추론의 진짜 위력입니다.

// ✏️ 직접 해보기 4 — 위 shop 에서 두 번째 메뉴의 이름을 출력해 보세요.


// ── 섹션 5: 그럼 언제 적어야 하나 ──

// 규칙은 한 줄로 정리됩니다.
//
//     초기값이 옆에 있으면 안 적는다. 없으면 적는다.
//
// 초기값이 없으면 타입스크립트가 볼 것이 없어서 알아낼 수가 없습니다.

// [적어야 하는 경우 1] 나중에 값이 정해지는 변수
let selectedMenu: string;
selectedMenu = "카페모카";
console.log(selectedMenu);
// 출력: 카페모카

// [적어야 하는 경우 2] 처음엔 비어 있고 나중에 채우는 것
//   05단원에서 null 을 배우면 이 경우를 제대로 다룹니다.

// [적어야 하는 경우 3] 함수의 매개변수 — 03단원에서 배웁니다.
//   이건 예외 없이 무조건 적어야 합니다. 추론할 근거가 아예 없기 때문입니다.

// [적어야 하는 경우 4] 객체의 모양을 미리 정해 둘 때 — 04단원에서 배웁니다.

// 그리고 이런 경우에도 '일부러' 적습니다.
// 값이 옆에 있어도, 내가 무엇을 기대하는지 남겨 두고 싶을 때입니다.
const total: number = 4000 + 4500;
console.log(total);
// 출력: 8500
//
// 01단원 개념01 섹션3에서 본 그것입니다.
// 적어 둔 자리에서만 걸리니, 중요한 계산 결과에는 적어 두는 편이 낫습니다.

// ✏️ 직접 해보기 5 — let count; 라고만 쓰고 count = 3; 을 한 뒤
//    count.toUpperCase() 를 써 보세요. 걸리나요? 왜 그럴까요?


// ── 섹션 6: 자주 하는 실수 ──

// [실수 1] 모든 곳에 타입을 적으려고 하기
//   초보자가 가장 많이 하는 것입니다. 코드만 길어지고 얻는 게 없습니다.
//   const price: number = 4000; 은 : number 가 하는 일이 없습니다.

// [실수 2] 타입을 안 적으면 검사를 안 한다고 생각하기
//   섹션 1을 보세요. 안 적어도 똑같이 막습니다.

// [실수 3] let 에 처음 넣은 값의 종류를 나중에 바꾸려 하기
//   let x = 5; 뒤에 x = "다섯"; 은 안 됩니다.
//   여러 종류를 담고 싶으면 05단원의 유니온이 필요합니다.

// [실수 4] const 의 타입이 number 라고 생각하기
//   const n = 5 의 타입은 number 가 아니라 5 입니다.
//   보통은 차이가 없지만 05단원에서 이 차이가 중요해집니다.


// ── 정리 ──

// 1. 값이 옆에 있으면 타입스크립트가 알아서 알아낸다. 이것이 추론이다.
// 2. 안 적어도 검사는 똑같이 한다. 적은 것과 효력이 같다.
// 3. 추론 결과를 보려면 마우스를 올리거나, 일부러 null 에 담아 에러를 읽는다.
// 4. const 는 값 자체가 타입(리터럴 타입), let 은 넉넉한 타입이 된다.
// 5. 객체·배열·중첩된 것까지 전부 추론한다. 자동완성도 그대로 된다.
// 6. 적어야 하는 곳은 '초기값이 없는 자리' 다.
//    그리고 함수 매개변수는 예외 없이 적는다(03단원).


// ============================================================
// 직접 해보기 정답
// ============================================================
//
// 1) error TS2551: Property 'toFixed' does not exist on type '"서울"'. Did you mean 'fixed'?
//    toFixed 는 숫자에만 있습니다.
//    Did you mean 'fixed'? 는 무시하세요. 문자열에 정말로 fixed 라는 옛날 기능이
//    있어서 비슷한 이름으로 골라 준 것입니다. 제안이 항상 옳지는 않습니다.
//    재현:
//    const city = "서울";
//    console.log(city.toFixed(2));
//    메시지에 'string' 이 아니라 '"서울"' 이라고 나오는 것에 주목하세요.
//    const 라서 리터럴 타입이 된 것입니다(섹션 3).
//
// 2) number[] 입니다.
//    마우스를 올리면 const stock: number[] 라고 뜹니다.
//    const 인데 왜 리터럴이 아니냐면, 배열 '안' 의 값은 나중에 바뀔 수 있기
//    때문입니다. const 가 막는 것은 stock 자체를 다른 배열로 바꾸는 것뿐입니다.
//    (JS자료 06단원에서 배운 그 내용입니다)
//
// 3) city = 3; 만 걸립니다.
//    error TS2322: Type 'number' is not assignable to type 'string'.
//    let 이라 타입이 string 으로 넉넉하게 잡혔고, 문자열끼리는 자유롭습니다.
//    재현:
//    let city = "서울";
//    city = "부산";
//    city = 3;
//
// 4) console.log(shop.menus[1].name);          // 출력: 라떼
//
// 5) 걸립니다.
//    error TS2339: Property 'toUpperCase' does not exist on type 'number'.
//    재현:
//    let count;
//    count = 3;
//    console.log(count.toUpperCase());
//    타입을 안 적었지만 count = 3; 을 보고 그 뒤부터는 number 로 취급합니다.
//    타입스크립트는 코드가 흘러가는 순서를 따라가며 타입을 좁혀 나갑니다.
//    이 성질이 05단원의 핵심이 됩니다.
