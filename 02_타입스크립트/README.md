# 타입스크립트 기초

`JS자료` → `React자료` 를 끝낸 학생을 위한 후속 자료입니다.
**9단원 / 58파일 / 약 37시간**(하루 4시간이면 10일).

---

## 시작하기 전에

### 준비물 두 가지

**① Node 22.18 이상.** 터미널에서 확인하세요.

```
node --version
```

`v22.18` 미만이면 [nodejs.org](https://nodejs.org) 에서 새로 설치하세요.
(22.18 부터 `node` 가 타입 표기를 옵션 없이 지웁니다. 그 아래 버전은 실행이 안 됩니다)
이 자료는 `node 파일.ts` 로 타입스크립트를 **바로 실행**합니다.
`ts-node` 니 `tsx` 니 하는 도구는 필요 없습니다.

**② VS Code.** 타입 에러는 편집기의 **빨간 물결 밑줄**로 보는 것이 기본입니다.
메모장으로 열면 이 자료의 절반이 안 보입니다.

### 한 번만 하는 준비

이 폴더에서:

```
npm install
```

**맥에서 자료를 옮겨 왔다면 한 번 더:**

```
npm run fix:names
```

맥은 한글 파일명을 NFD(자모 분리)로 저장합니다. 글자로는 똑같이 보이는데 바이트가 다릅니다.
압축을 풀거나 옮기기만 해도 이렇게 바뀌고, 그러면 검증도구가 파일을 못 찾아
**`npm run typecheck` 가 안 푼 연습문제까지 검사해서 시끄러워집니다.**
검사 결과가 이상하면 이것부터 돌리세요. 고칠 게 없으면 조용히 끝납니다.

07단원에 들어갈 때 한 번 더:

```
cd 07_React와_타입스크립트/실습프로젝트
npm install
```

---

## 명령 네 개

JS자료에서는 `node 파일.js` 하나였습니다. 여기서는 넷입니다.

| 명령 | 하는 일 | 언제 |
| --- | --- | --- |
| `node 파일.ts` | **실행만** 한다. 타입은 안 본다 | 개념 파일을 돌려 볼 때 |
| `npm run typecheck` | 개념·정답 파일을 검사한다 | **언제나 조용해야 정상** |
| | (`npx tsc --noEmit` 과 같은 것입니다. 짧게 쓰려고 등록해 둔 이름입니다) | |
| `npm run grade` | 연습문제를 **채점**한다 | 문제를 풀면서 (**전부 일치하면 다 맞은 것**) |
| `npm run check` | 연습문제를 타입 검사한다 | 풀면서 같이 (일부 문제만 잡습니다) |
| `npm run fix:names` | 한글 파일명을 NFC 로 되돌린다 | 자료를 옮긴 뒤 검사가 이상할 때 |

> **채점은 `npm run grade` 로 합니다.**
> 연습문제를 실제로 돌려서 문제에 적힌 '기대 출력' 과 줄 단위로 맞춰 봅니다.
>
> `npm run check`(타입 검사)만 믿으면 안 됩니다. **138문항 중 21개(15%)만 잡습니다.**
> "예상한 값을 출력하세요" 같은 문제는 안 풀어도 타입 에러가 안 나기 때문입니다.
> 02단원과 09단원 종합01~03은 타입 검사로 잡히는 문제가 아예 없습니다.
> 어느 문제가 어느 쪽으로 잡히는지는 `npm run audit:exercises` 로 볼 수 있습니다.
>
> 07단원과 09단원 종합04는 브라우저라 자동 채점이 없습니다. '기대 화면' 을 눈으로 확인하세요.

> **이 자료에서 제일 중요한 것** — `node` 는 타입을 검사하지 않고 **지웁니다**.
> 그래서 타입이 틀려도 화면은 그냥 돌아갑니다.
> 이걸 모르면 반드시 헤맵니다. 01단원 개념02가 이 이야기만 합니다.

`tsc` 가 아무 말도 안 하면 통과입니다. "성공" 같은 글자는 안 나옵니다.

---

## 단원

| 단원 | 내용 | 시간 |
| --- | --- | --- |
| 01 타입스크립트 시작하기 | 왜 타입인가 · **실행과 검사는 따로다** · 에러 메시지 읽는 법 | 3.4h |
| 02 기본 타입과 추론 | string·number·boolean·배열 · **튜플** · 추론 · **any 금지** | 3.6h |
| 03 함수 타입 | 매개변수·반환값 · 선택 매개변수·기본값 · 콜백 | 3.3h |
| 04 객체와 타입 별칭 | 객체 타입 · **인덱스 시그니처** · `type` · 선택 속성·`interface` · **서버 데이터** | 4.5h |
| 05 유니온과 타입 좁히기 | 유니온 · 좁히기 · `null`/`undefined` · **판별 유니온** · **타입 가드 `a is T`** | 4.6h |
| 06 제네릭 | 왜 필요한가 · 쓰는 법 · 이미 쓰고 있던 제네릭 · **`keyof`** | 4.4h |
| 07 React와 타입스크립트 | props · useState · 이벤트·폼 · 화면 상태 | 3.6h |
| 08 기존 프로젝트에 타입 입히기 | `as` 금지 · JS 옮기기 · tsconfig · **타입 없는 패키지** · **`declare module`** | 4.5h |
| **09 종합 실습** | **여러 단원을 엮어 넷을 만든다** (주문 관리 · 재고 · 서버 응답 · React 화면) | **5.2h** |

01~06·08단원은 `.ts` 파일이고 `node` 로 바로 돌립니다.
07단원과 09단원의 종합04만 브라우저가 필요해서 `실습프로젝트/` 안에 있습니다.

09단원에는 개념 파일이 없습니다. 배운 것을 조립하는 실습만 넷입니다.
`09_종합_실습/README.md` 를 보세요.

---

## 파일 읽는 법

각 단원 폴더는 이렇게 생겼습니다.

```
04_객체와_타입_별칭/
  개념01_객체_타입_적기.ts     ← 읽고 따라 치는 파일
  개념02_type_별칭.ts
  개념03_선택속성과_interface.ts
  개념04_서버_데이터에_모양_적기.ts
  연습문제.ts                  ← 직접 푸는 파일
  연습문제_정답.ts             ← 해설이 본체입니다

09_종합_실습/                  ← 개념 파일이 없습니다
  종합01_주문_관리.ts          ← 여러 단원을 엮는 실습
  종합01_주문_관리_정답.ts
  ...
```

개념 파일 안의 표시:

| 표시 | 뜻 |
| --- | --- |
| `── 섹션 N: 제목 ──` | 한 덩어리의 시작 |
| `// 출력:` | 그 위 `console.log` 가 실제로 찍는 것 |
| `// 에러: TS2322 ...` | 아래 주석 처리된 줄의 `//` 를 지우면 나는 에러 |
| `// ✏️ 직접 해보기 N` | 손으로 해 보는 자리. 정답은 파일 맨 아래 |
| `── 자주 하는 실수 ──` | 실제로 많이 나는 실수 |
| `── 정리 ──` | 그 파일의 요약 |

**`// 에러:` 가 보이면 주석을 풀어서 직접 확인해 보세요.**
빨간 밑줄이 어떻게 생기는지 한 번 보는 것이 설명 열 줄보다 낫습니다.
확인했으면 다시 `//` 를 붙이세요.

---

## 이 자료가 정한 규칙 셋

세 가지를 **쓰지 않습니다.** 셋 다 "검사를 끄는 도구" 입니다.

| 금지 | 왜 | 어디서 |
| --- | --- | --- |
| `any` | 검사를 통째로 끈다. 그리고 옆으로 번진다 | 02단원 개념03 |
| `!` | "없을 리 없다" 고 우기는 것. 있으면 터진다 | 05단원 개념03 |
| `as` | 값을 안 바꾸고 생각만 바꾼다 | 08단원 개념01 |

셋 다 **"검사는 통과하는데 돌리면 터진다"** 를 만듭니다.
이게 타입스크립트를 쓰는 이유를 정면으로 없앱니다.

이 셋 중 하나를 쓰고 싶어질 때는 대개 아직 안 배운 것이 있다는 신호입니다.

- 모양을 못 적겠다 → **04단원**
- 확인을 못 하겠다 → **05단원**
- 타입마다 같은 함수를 또 만들고 있다 → **06단원**

---

## 자주 보게 될 에러 다섯 개

| 번호 | 뜻 |
| --- | --- |
| `TS2322` | 담을 수 없다 (`Type 'A' is not assignable to type 'B'`) |
| `TS2339` | 그런 이름이 없다 |
| `TS2551` | 그런 이름이 없다 + 이거 아니냐 |
| `TS2345` | 넘긴 값이 안 맞는다 |
| `TS18048` / `TS18047` | `undefined` / `null` 일 수도 있다 |

`Type 'A' is not assignable to type 'B'` 에서
**A 는 내가 넣은 것, B 는 들어가야 하는 것**입니다. 거꾸로 읽으면 엉뚱한 곳을 고칩니다.

자세한 것은 01단원 개념03에 있습니다.

---

## 자료를 고칠 때 (강사용)

내용을 손보면 **반드시** 검증기를 돌리세요. 이 자료는 검증기가 절반입니다.

```
npm run verify:all      # 아래를 한 번에
npm run verify          # 출력 대조 + 에러 대조 + ✏️ 정답 대조 + 전체 타입 검사
npm run verify:claims   # 정답 블록의 'error TSxxxx' 주장 48건을 실제로 돌려 대조
npm run audit           # 구조 감사 (번호 연속성 · 정답 누락 · 섹션 커버리지)
npm run audit:exercises # 채점 기준 확인 (에러가 전부 TODO 안에서 나는가)
npm run grade -- --answer  # 문제의 기대 출력 ↔ 정답의 실제 출력 대조
npm run forward         # 안 배운 문법이 앞 단원에 새는지
npm run stats           # 분량 · 수업 시간 재산정
```

07단원은 따로입니다.

```
node _검증도구/verify_tsx.js      # 에러 예제 대조
node _검증도구/drive_07.js        # 진짜 크롬으로 열어 조작까지 (dev 서버를 먼저 켤 것)
```

자세한 것은 `_검증도구/README.md` 를 보세요.

---

## 이 자료가 안 다루는 것

일부러 뺐습니다. 지금 외워도 쓸 자리가 없어서 잊어버립니다.

- 조건부 타입 · 매핑된 타입 (이른바 '타입 체조')
- 유틸리티 타입 `Partial` · `Pick` · `Omit` · `Record` (이름만 08단원에서 소개)
- `enum` (아예 막아 두었습니다. 05단원의 리터럴 유니온으로 대신합니다)
- 데코레이터 · 네임스페이스

**다음에 볼 것** — `zod`(밖에서 온 값 확인) · `ESLint`(규칙 자동 검사) · 유틸리티 타입.
08단원 개념03에 정리해 두었습니다.

---

## 시리즈 지도

| PART | 폴더 | 내용 |
| --- | --- | --- |
| 1 | `HTMLCSS자료` | HTML·CSS — 자바스크립트가 안 나옵니다 (15단원) |
| 1 | `JS자료` | 자바스크립트 기초 — 변수부터 fetch 까지 (15단원) |
| 2 | `React자료` | React·Vite·Router — 화면 만들기 (16단원) |
| 3 | `백엔드자료` | Node·Express·REST·미들웨어·업로드 (12단원) |
| 4 | `클라우드자료` | SQL·Supabase·EC2·PM2·S3·배포·로그인 (11단원) |
| 5 | `AI자료` | LLM·프롬프트·스트리밍·문서 QA (7단원) |
| 6 | `(준비 중)` | 통합 프로젝트 — 위의 것을 하나로 묶습니다 |
| 선택 | **`TS자료`** | 타입스크립트 — PART 2 뒤 언제든 (9단원)  ← **이 자료** |
| 선택 | `DB자료` | 데이터베이스 심화 — 단독으로도 완결됩니다 (10단원) |

PART 1~6 은 순서대로 봅니다. **선택** 두 개는 아무 때나 봐도 되고, 안 봐도 됩니다.

- `TS자료` 는 React자료 15단원(부록)을 더 깊게 다룬 것입니다.
- `DB자료` 는 클라우드자료 02~03단원의 SQL 을 데이터베이스 쪽에서 깊게 다시 봅니다.
  둘 다 봐도 되고, 하나만 봐도 됩니다.
