// ============================================================
// 05단원 연습문제 — 유니온과 타입 좁히기
// ------------------------------------------------------------
// 실행: node 연습문제.ts
// 채점: npm run grade          ← 출력을 기대 출력과 맞춰 봅니다
// 검사: npm run check          ← 타입 검사 (일부 문제만 잡습니다)
// ============================================================
//
// ★ 채점은 npm run grade 로 합니다.
//   실제 출력을 문제의 '기대 출력' 과 줄 단위로 맞춰 봅니다.
//   전부 일치하면 다 맞은 것입니다.
//
//   npm run check(타입 검사)도 같이 쓰세요. 다만 이쪽은 '일부 문제만' 잡습니다.
//   138문항 중 21개뿐입니다. "출력하세요" 류는 안 풀어도 타입 에러가 안 나거든요.
//   그러니 check 가 조용하다고 다 맞은 것이 아닙니다. grade 로 확인하세요.
//   (npm run typecheck 는 개념·정답 파일만 봅니다. 그쪽은 언제나 조용합니다)
//
// 푸는 방법
//   1. TODO 자리에 코드를 씁니다.
//   2. npm run grade 로 채점합니다. (직접 node 연습문제.ts 로 돌려 봐도 됩니다)
//   3. npm run check 로 타입도 봅니다.
//   4. 10분 고민해도 안 되면 연습문제_정답.ts 를 보세요.
//
// 문제 1~8은 기본, 9~14는 응용, 15는 [도전]입니다.
// 문제 16은 에러를 직접 보는 문제라 맨 뒤에 있습니다.

console.log("=== 05단원 연습문제 ===");


// ───── 문제 1 ───── (개념01 섹션1)
// 문자열이거나 숫자인 타입 Id 를 만들고, 두 종류를 각각 넘겨 출력하세요.
//
// 기대 출력:
// 3
// A-3
//
// 기대 검사 결과: 조용함
{
  // TODO: type Id 를 만들고 함수 하나를 만들어 두 번 부르세요
}


// ───── 문제 2 ───── (개념01 섹션3)
// "S" | "M" | "L" 만 받는 Size 타입을 만들고 함수에 넘기세요.
//
// 기대 출력:
// 사이즈: M
//
// 기대 검사 결과: 조용함
{
  // TODO: type Size 를 만들고 printSize("M") 을 부르세요
}


// ───── 문제 3 ───── (개념02 섹션1)
// 아래 함수는 걸립니다. typeof 로 확인해서 걸리지 않게 고치세요.
// 문자열이면 글자 수, 숫자면 소수 첫째 자리까지 출력하세요.
//
// 기대 출력:
// 5
// 4500.0
//
// 기대 검사 결과: 조용함
{
  // TODO: typeof 로 확인해 고치세요
  function describe(value: string | number) {
    console.log(value.length);
  }
  // describe("아메리카노");
  // describe(4500);
}


// ───── 문제 4 ───── (개념02 섹션2)
// Status 에 따라 다른 문구를 돌려주는 함수를 switch 로 만드세요.
// 반환 타입 : string 을 꼭 적으세요.
//
// 기대 출력:
// 만들고 있어요
//
// 기대 검사 결과: 조용함
{
  type Status = "대기" | "조리중" | "완료";

  // TODO: switch 로 만들고 "조리중" 을 넘겨 출력하세요
  //       ("대기"→"곧 시작합니다" / "조리중"→"만들고 있어요" / "완료"→"나왔습니다")
}


// ───── 문제 5 ───── (개념02 섹션3)
// 하나면 그대로, 배열이면 합계를 출력하는 함수를 만드세요.
//
// 기대 출력:
// 5
// 6
//
// 기대 검사 결과: 조용함
{
  // TODO: number | number[] 를 받는 sumAll 을 만들고 5 와 [1,2,3] 을 넘기세요
}


// ───── 문제 6 ───── (개념03 섹션3)
// ?. 를 써서 owner 가 없어도 터지지 않게 출력하세요.
//
// 기대 출력:
// 홍길동
// undefined
//
// 기대 검사 결과: 조용함
{
  type Shop = { name: string; owner: { name: string } | null };
  const shop1: Shop = { name: "봄날카페", owner: { name: "홍길동" } };
  const shop2: Shop = { name: "무인카페", owner: null };

  // TODO: 두 가게의 주인 이름을 ?. 로 출력하세요
}


// ───── 문제 7 ───── (개념03 섹션4)
// ?? 를 써서 주인이 없으면 "주인 없음" 이 나오게 하세요.
//
// 기대 출력:
// 홍길동
// 주인 없음
//
// 기대 검사 결과: 조용함
{
  type Shop = { name: string; owner: { name: string } | null };
  const shop1: Shop = { name: "봄날카페", owner: { name: "홍길동" } };
  const shop2: Shop = { name: "무인카페", owner: null };

  // TODO: ?. 와 ?? 를 함께 써서 출력하세요
}


// ───── 문제 8 ───── (개념04 섹션2)
// kind 를 표딱지로 삼는 판별 유니온을 만들고, switch 로 소리를 출력하세요.
//
// 기대 출력:
// 멍멍
// 야옹
//
// 기대 검사 결과: 조용함
{
  // TODO: Dog({kind:"dog", bark}) 와 Cat({kind:"cat", meow}) 을 만들고
  //       speak 함수를 switch 로 만들어 둘 다 출력하세요
}


// ───── 문제 9 ───── (응용 · 개념01 섹션2)
// 아래 코드가 왜 걸리는지 이유를 한 줄로 출력하세요.
//
//     function f(v: string | number) { return v.toUpperCase(); }
//
// 기대 출력:
// 숫자에는 toUpperCase 가 없어서
//
// 기대 검사 결과: 조용함
{
  // TODO: 이유를 그대로 출력하세요
}


// ───── 문제 10 ───── (응용 · 개념02 섹션4)
// 아래 함수는 수량 0 을 "없음" 으로 잘못 처리합니다. 고치세요.
//
// 기대 출력:
// 수량: 3
// 수량: 0
// 수량 없음
//
// 기대 검사 결과: 조용함
{
  // TODO: 0 도 있는 값으로 처리되게 고치세요
  function showCount(count?: number) {
    if (count) {
      console.log("수량:", count);
    } else {
      console.log("수량 없음");
    }
  }
  showCount(3);
  showCount(0);
  showCount();
}


// ───── 문제 11 ───── (응용 · 개념03 섹션4)
// 아래 두 줄의 결과를 순서대로 출력하세요.
//
//     const n: number | null = 0;
//     console.log(n ?? 99);
//     console.log(n || 99);
//
// 기대 출력:
// 0
// 99
//
// 기대 검사 결과: 조용함
{
  // TODO: 두 결과를 출력하세요
}


// ───── 문제 12 ───── (응용 · 개념03 섹션5)
// 아래 코드는 검사를 통과하지만 실행하면 터집니다.
// ! 를 쓰지 않고 안전하게 고치세요. 없으면 "없음" 을 출력하세요.
//
// 기대 출력:
// 없음
//
// 기대 검사 결과: 조용함
{
  type Shop = { owner: { name: string } | null };
  const shop: Shop = { owner: null };

  // TODO: ! 를 쓰지 않고 고치세요
  // console.log(shop.owner!.name);
}


// ───── 문제 13 ───── (응용 · 개념04 섹션5)
// 아래 코드는 걸립니다. 표딱지(kind) 값의 타입을 고쳐서 통과하게 만드세요.
//
// 기대 출력:
// 멍멍
//
// 기대 검사 결과: 조용함
{
  // TODO: kind 의 타입을 고치세요 (힌트: string 말고 그 값 자체를 적습니다)
  type Dog = { kind: string; bark: string };
  type Cat = { kind: string; meow: string };
  type Animal = Dog | Cat;

  function speak(a: Animal) {
    if (a.kind === "dog") {
      return a.bark;
    }
    return "";
  }
  // console.log(speak({ kind: "dog", bark: "멍멍" }));
}


// ───── 문제 14 ───── (응용 · 개념02 섹션5)
// 아래 함수를 '이른 반환' 으로 바꿔서, 아래쪽 두 줄이 전부 문자열로 취급되게 하세요.
//
// 기대 출력:
// LATTE
// 5
//
// 기대 검사 결과: 조용함
{
  // TODO: 숫자면 먼저 return 하고, 아래에서 문자열 기능을 쓰세요
  function process(value: string | number) {
    console.log(value);
  }
  // process("latte");
}


// ───── 문제 15 ───── ([도전] · 개념01~04 종합)
// 화면 상태를 판별 유니온으로 만들고, 세 상태를 각각 출력하세요.
//   ① LoadState 를 만든다
//        "로딩중"  → 추가 정보 없음
//        "성공"    → data: string[]
//        "실패"    → message: string
//   ② render(state): string 를 switch 로 만든다
//   ③ 세 상태를 각각 넘겨 출력한다
//
// 기대 출력:
// 불러오는 중...
// 2개: 아메리카노, 라떼
// 오류: 서버 없음
//
// 기대 검사 결과: 조용함
{
  // TODO: 위 세 가지를 하세요
}


// ───── 문제 16 ───── (에러 확인 · 개념03 섹션5)
// 아래 세 줄의 // 를 지우고,
//
//   ① npm run check 로 검사해 보세요   → 걸릴까요?
//   ② node 연습문제.ts 로 실행해 보세요 → 어떻게 될까요?
//
// ! 를 쓰면 무슨 일이 생기는지 직접 보는 문제입니다.
// 확인했으면 다시 // 를 붙이세요.
{
  // type Shop = { owner: { name: string } | null };
  // const shop: Shop = { owner: null };
  // console.log(shop.owner!.name);
}
