// ============================================================
// 03단원 심화문제 — 조건문
// ------------------------------------------------------------
// 실행: node 심화문제.js
// ============================================================
//
// [이건 선택 과제입니다]
//   연습문제를 다 푼 사람만 하세요. 안 풀어도 다음 단원에 지장 없습니다.


// ───── 심화 1 ───── 조건 순서가 틀리면 어떻게 되나
// 아래는 '일부러 순서를 틀리게' 쓴 등급 판정입니다.
// 먼저 이 코드가 무엇을 출력할지 예상해 보고, 실행해서 확인하세요.
// 그다음 순서를 고쳐서 올바른 등급이 나오게 하세요.

const score1 = 95;

// [틀린 코드] — 이대로 두고 먼저 실행해 보세요
if (score1 >= 60) {
  console.log("틀린 결과: D");
} else if (score1 >= 70) {
  console.log("틀린 결과: C");
} else if (score1 >= 80) {
  console.log("틀린 결과: B");
} else if (score1 >= 90) {
  console.log("틀린 결과: A");
}
// 출력: 틀린 결과: D

// 95점인데 D가 나왔습니다. 왜 그럴까요?
// 아래에 순서를 고친 코드를 써서 "올바른 결과: A" 가 나오게 하세요.
//
// 기대 출력:
// 올바른 결과: A

// TODO: 여기에 코드를 쓰세요


// ───── 심화 2 ───── switch 흘러내림을 일부러 쓰기
// 월(1~12)을 받아 계절을 출력하는 코드를 switch 로 만드세요.
// if 를 쓰지 말고, case 를 묶는 방식(개념04 섹션 3)을 쓰세요.
//
//   12, 1, 2   → 겨울
//   3, 4, 5    → 봄
//   6, 7, 8    → 여름
//   9, 10, 11  → 가을
//
// 아래 세 값으로 각각 확인하세요. 같은 코드를 세 번 쓰지 말고
// month 값만 바꿔 가며 확인해도 되고, 03·04단원을 배운 뒤라면 다르게 해도 됩니다.
// 여기서는 세 번 써도 괜찮습니다.

const month1 = 1;
const month2 = 7;
const month3 = 10;

// 기대 출력:
// 겨울
// 여름
// 가을

// TODO: 여기에 코드를 쓰세요


// ───── 심화 3 ───── 안전하게 확인하기
// 아래 세 값이 각각 "쓸 수 있는 글자인가" 를 판정하세요.
// 조건은 세 가지를 모두 만족해야 합니다.
//   ① 값이 있어야 하고 (null·undefined 가 아니어야 하고)
//   ② 문자열이어야 하고
//   ③ 공백만 있는 것도 아니어야 합니다
//
// 힌트: 개념03의 && 안전 검사와 01단원의 typeof 를 함께 씁니다.
//       공백만 있는지는 trim() 을 쓰면 되는데, 아직 안 배웠습니다.
//       대신 " " 와 직접 비교해도 이 문제는 풀립니다.

const input1 = "안녕하세요";
const input2 = " ";
const input3 = null;

// 기대 출력:
// 사용 가능
// 사용 불가
// 사용 불가

// TODO: 여기에 코드를 쓰세요
