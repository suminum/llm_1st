// ============================================================
// 09단원 심화문제 — 구조분해와 스프레드
// ------------------------------------------------------------
// 실행: node 심화문제.js
// ============================================================
//
// [이건 선택 과제입니다]
//   연습문제를 다 푼 사람만 하세요. 안 풀어도 다음 단원에 지장 없습니다.


// ───── 심화 1 ───── 한 줄에 네 가지를 다 넣기
// 아래 order 에서 다음 네 가지를 '구조분해 한 번' 으로 꺼내세요.
//
//   ① id
//   ② customer 안의 name 을 customerName 이라는 이름으로
//   ③ customer 안의 address 안의 city (없으면 "미입력")
//   ④ items 배열의 첫 번째 항목의 name 을 firstItem 이라는 이름으로
//
// 힌트: 중첩 구조분해 · 이름 바꾸기 · 기본값 · 배열 구조분해를 한꺼번에 씁니다.
//       ③은 address 가 아예 없을 수도 있으니 = {} 로 막아야 합니다. (개념02 섹션 7)
//       ④는 객체 안의 배열이라 items: [ { name: firstItem } ] 모양이 됩니다.

const order = {
  id: 100,
  customer: {
    name: "정하늘",
    address: { city: "부산" },
  },
  items: [
    { name: "라떼", price: 4500 },
    { name: "쿠키", price: 3000 },
  ],
};

// 기대 출력:
// 100 정하늘 부산 라떼

// TODO: 여기에 코드를 쓰세요
// (console.log 한 번으로 네 개를 쉼표로 나열해 찍으세요)


// ───── 심화 2 ───── 깊은 곳만 바꾼 새 객체
// 아래 state 에서 cart 의 id 2 번 항목의 count 만 5 로 바꾼
// '새 state' 를 만드세요. 원본은 하나도 바뀌면 안 됩니다.
//
// 힌트: 겉 → 안 순서로 한 겹씩 새로 만듭니다.
//   ① { ...state, cart: 새로운cart }
//   ② 새로운cart 는 state.cart.map(...) 으로 만듭니다
//   ③ map 안에서 id 가 2 인 것만 { ...item, count: 5 } 로 바꾸고 나머지는 그대로
//
// 마지막에 원본의 count 도 함께 찍어서 안 바뀌었는지 확인하세요.

const state = {
  user: { name: "김민준", point: 100 },
  cart: [
    { id: 1, count: 2 },
    { id: 2, count: 1 },
  ],
};

// 기대 출력:
// 새 state 의 2번 count: 5
// 원본의 2번 count: 1
// user 객체를 그대로 재사용했나: true

// TODO: 여기에 코드를 쓰세요
// (마지막 줄은 newState.user === state.user 를 찍으세요)


// ───── 심화 3 ───── 몇 개가 오든 받아 내는 함수
// 이름표와 숫자 여러 개를 받아 아래 형태로 돌려주는 summarize 함수를 만드세요.
//
//   summarize("1반", 10, 20, 30)  →  "1반 — 합계 60 / 평균 20 / 최대 30"
//   summarize("2반", 5)           →  "2반 — 합계 5 / 평균 5 / 최대 5"
//   summarize("3반")              →  "3반 — 자료 없음"
//
// 조건:
//   - 숫자는 몇 개가 와도 되어야 합니다 (나머지 매개변수)
//   - 평균은 소수 첫째 자리까지 (toFixed(1) 은 문자열을 돌려줍니다)
//   - 하나도 안 오면 "자료 없음"
//
// 힌트: 최대값은 Math.max 에 스프레드로 펼쳐 넣습니다. (개념03 섹션 5)
//       합계는 reduce 를 쓰면 한 줄입니다. (08단원)

// 기대 출력:
// 1반 — 합계 60 / 평균 20.0 / 최대 30
// 2반 — 합계 5 / 평균 5.0 / 최대 5
// 3반 — 자료 없음

// TODO: 여기에 코드를 쓰세요
