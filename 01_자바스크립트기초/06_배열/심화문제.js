// ============================================================
// 06단원 심화문제 — 배열
// ------------------------------------------------------------
// 실행: node 심화문제.js
// ============================================================
//
// [이건 선택 과제입니다]
//   연습문제를 다 푼 사람만 하세요. 안 풀어도 다음 단원에 지장 없습니다.


// ───── 심화 1 ───── 중복 제거
// 아래 배열에서 중복을 없앤 새 배열을 만드세요. 원본은 그대로 두세요.
// 순서는 처음 나온 순서를 유지해야 합니다.
//
// 힌트: 빈 배열을 하나 만들고, 원본을 돌면서
//       "새 배열에 아직 없으면 넣는다" 를 반복합니다. (개념04의 includes)

const numbers = [3, 1, 4, 1, 5, 9, 2, 5, 3];

// 기대 출력:
// [ 3, 1, 4, 5, 9, 2 ]
// 원본 9개 → 중복 제거 6개

// TODO: 여기에 코드를 쓰세요


// ───── 심화 2 ───── 교집합과 차집합
// 두 배열을 비교해서 세 가지를 각각 배열로 만들어 출력하세요.
//
//   ① 양쪽에 다 있는 것        (교집합)
//   ② a 에만 있는 것
//   ③ b 에만 있는 것
//
// 힌트: includes 로 "상대편에 있는가" 를 물어보면 됩니다.

const a = ["사과", "바나나", "포도", "딸기"];
const b = ["바나나", "딸기", "수박"];

// 기대 출력:
// 둘 다: [ '바나나', '딸기' ]
// a에만: [ '사과', '포도' ]
// b에만: [ '수박' ]

// TODO: 여기에 코드를 쓰세요


// ───── 심화 3 ───── 배열 회전
// 배열을 오른쪽으로 2칸 밀어 보세요. 뒤로 밀려난 것은 앞으로 돌아옵니다.
//
//   [1, 2, 3, 4, 5]  →  오른쪽 2칸  →  [4, 5, 1, 2, 3]
//
// 원본은 바뀌면 안 됩니다. 마지막에 원본도 함께 출력해 확인하세요.
//
// 힌트: 두 가지 방법이 있습니다. 하나만 성공하면 됩니다.
//   ① slice 로 뒤 2개와 앞 3개를 각각 잘라 이어 붙이기
//      (slice 는 음수를 넣을 수 있습니다. slice(-2) 는 뒤에서 2개)
//   ② 복사본을 만든 뒤 pop 한 것을 unshift 로 앞에 넣기를 2번 반복
//
//   ①이 짧지만, ②가 무슨 일이 일어나는지 더 잘 보입니다.

const items = [1, 2, 3, 4, 5];

// 기대 출력:
// [ 4, 5, 1, 2, 3 ]
// 원본: [ 1, 2, 3, 4, 5 ]

// TODO: 여기에 코드를 쓰세요
