# 줄바꿈은 LF 로 통일합니다.
#
# 윈도우에서 편집하면 편집기가 CRLF 로 바꿔 놓는 일이 있습니다.
# 그러면 한 글자도 안 고쳤는데 git 이 "파일 전체가 바뀌었다" 고 봅니다.
# 이 파일이 그걸 막습니다.
* text=auto eol=lf

# 아래는 그대로 두는 것(바이너리)
*.db  binary
*.png binary
*.jpg binary
*.ico binary
