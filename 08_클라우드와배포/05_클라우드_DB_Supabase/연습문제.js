// ============================================================
// 05단원 연습문제 — 클라우드 DB (Supabase)
// ------------------------------------------------------------
// 실행: node 연습문제.js
// ============================================================
//
// TODO 자리에 코드를 쓰고, '기대 출력'과 같은지 확인하세요.
// 1~10은 기본, 11~14는 응용, 15는 [도전] 입니다.
//
// ★ 인터넷 없이 풀 수 있습니다. 가짜Supabase.js 가 대답합니다.
//   대신 **요청 주소는 진짜 클라이언트가 만든 것** 이라 그대로 믿어도 됩니다.
//
// ★★ 마지막에 'SQL 로 써 볼 것' 이 있습니다.
//   Supabase 프로젝트를 만들었으면 SQL Editor 에서 직접 쳐 보세요.
//   그건 코드로 확인할 수 없습니다.

const { 기록하는클라이언트, 표있는클라이언트 } = require("./가짜Supabase");

const 처음자료 = [
  { id: 1, name: "용접로봇 1호", line: "A", status: "가동", temperature: 36.5 },
  { id: 2, name: "프레스 1호", line: "A", status: "정지", temperature: 41.2 },
  { id: 3, name: "프레스 2호", line: "B", status: "점검중", temperature: 39.8 },
  { id: 4, name: "컨베이어 1호", line: "B", status: "가동", temperature: null },
  { id: 5, name: "검사기 1호", line: "C", status: "가동", temperature: 22.4 },
];


// ───── 문제 1 ───── (개념03)
// A 라인의 가동 중인 설비 이름을 id 순으로 출력하세요.
//
// 힌트: .eq() 를 두 번 이어 붙이면 AND 입니다.
//
// 기대 출력:
// 용접로봇 1호

async function 문제1(sb) {
  // TODO: 여기에 코드를 쓰세요
}


// ───── 문제 2 ───── (개념03)
// 아래 조회가 만드는 REST 주소를 확인하세요.
// 기록하는클라이언트 를 써서 주소를 찍으면 됩니다.
//
// 기대 출력:
// GET /equipments?select=id,name&line=eq.B&temperature=gte.30&order=temperature.desc,id.asc&limit=2

async function 문제2() {
  const { 클라이언트, 기록 } = 기록하는클라이언트();
  // TODO: 위 주소가 나오도록 조회를 만드세요
  // TODO: 기록[0] 을 "방법 주소" 형태로 출력하세요
}


// ───── 문제 3 ───── (개념03)
// id 가 1 인 설비 하나를 꺼내되, 없으면 null 이 되게 하세요.
// 오류가 나면 안 됩니다.
//
// 힌트: single 과 maybeSingle 중 어느 쪽일까요?
//
// 기대 출력:
// 용접로봇 1호
// null

async function 문제3(sb) {
  // TODO: 여기에 코드를 쓰세요
}


// ───── 문제 4 ───── (개념03)
// 설비를 하나 넣고, 만들어진 id 를 출력하세요.
// 조회를 두 번 하면 안 됩니다.
//
// 기대 출력:
// id=6 status=정지

async function 문제4(sb) {
  // TODO: 여기에 코드를 쓰세요
}


// ───── 문제 5 ───── (개념03)
// 아래 코드는 만든 줄을 못 받습니다. 왜 그런지 확인하고 고치세요.
//
// 기대 출력:
// 고치기 전: null / 204
// 고친 뒤: 포장기 1호

async function 문제5(sb) {
  const 결과 = await sb.from("equipments").insert({ name: "포장기 1호", line: "C" });
  console.log(`고치기 전: ${결과.data} / ${결과.status}`);

  // TODO: 만든 줄이 오게 고쳐서 name 을 출력하세요
}


// ───── 문제 6 ───── (개념03)
// id 3 을 지우고, 없는 id 9999 도 지워 보세요.
// 각각 몇 건이 지워졌는지 출력하세요.
//
// 기대 출력:
// 3 지움: 1건
// 9999 지움: 0건

async function 문제6(sb) {
  // TODO: 여기에 코드를 쓰세요
}


// ───── 문제 7 ───── (개념03)
// 이름 중복으로 오류가 나게 하고, 오류 코드와 status 를 출력하세요.
//
// 기대 출력:
// code=23505 status=409

async function 문제7(sb) {
  // TODO: 여기에 코드를 쓰세요
}


// ───── 문제 8 ───── (개념03)
// 오류를 확인하지 않으면 어떤 일이 생기는지 직접 보세요.
// 중복 insert 의 data 를 그대로 쓰면 어떻게 되나요?
//
// 기대 출력:
// data 는 null 입니다
// TypeError 가 납니다

async function 문제8(sb) {
  // TODO: 중복 insert 를 하고 data 가 null 인지 출력하세요
  // TODO: data.length 를 try/catch 로 감싸서 TypeError 를 확인하세요
}


// ───── 문제 9 ───── (개념02)
// 아래 SQLite 조회를 Postgres 용으로 고치세요. (문자열로 출력만 하면 됩니다)
//
//   SELECT line, group_concat(name, ', ') AS names
//   FROM equipments
//   WHERE name LIKE '%ROBOT%'
//   GROUP BY line
//   ORDER BY line
//
// 바꿔야 할 것 두 가지가 있습니다.
//
// 기대 출력:
// string_agg 를 씁니다
// ilike 를 씁니다

function 문제9() {
  // TODO: 무엇을 바꿔야 하는지 두 줄로 출력하세요
}


// ───── 문제 10 ───── (개념02)
// 온도 오름차순으로 정렬할 때, NULL 이 맨 뒤에 오게 하세요.
// SQLite 와 Postgres 에서 결과가 같아야 합니다.
//
// 힌트: .order() 의 두 번째 인자에 옵션이 있습니다.
//
// 기대 출력:
// GET /equipments?select=id&order=temperature.asc.nullslast

async function 문제10() {
  const { 클라이언트, 기록 } = 기록하는클라이언트();
  // TODO: 여기에 코드를 쓰세요
}


// ───── 문제 11 ───── (개념04, 응용)
// RLS 로 읽기가 막힌 상태에서 조회하면 무엇이 오는지 확인하세요.
// 그리고 쓰기가 막힌 상태의 오류도 확인하세요.
//
// 힌트: 표있는클라이언트(자료, { 읽기막힘: true }) 로 만들 수 있습니다.
//
// 기대 출력:
// 읽기 막힘: data=[] error=null
// 쓰기 막힘: code=42501 status=403

async function 문제11() {
  // TODO: 여기에 코드를 쓰세요
}


// ───── 문제 12 ───── (개념03, 응용)
// N+1 을 없애세요.
// 아래는 설비마다 점검을 따로 조회합니다. 한 번의 요청으로 바꾸세요.
//
// 힌트: .select("*, checks(id)") 처럼 중첩해서 적을 수 있습니다.
//
// 기대 출력:
// N+1 요청 수: 6
// 고친 뒤 요청 수: 1

async function 문제12() {
  const { 클라이언트: sb, 기록 } = 기록하는클라이언트([{ id: 1 }, { id: 2 }, { id: 3 }, { id: 4 }, { id: 5 }]);

  // N+1 방식
  const 설비들 = (await sb.from("equipments").select("id")).data;
  for (const 설비 of 설비들) {
    await sb.from("checks").select("id").eq("equipment_id", 설비.id);
  }
  console.log("N+1 요청 수:", 기록.length);

  // TODO: 기록을 비우고(기록.length = 0), 한 번의 요청으로 같은 것을 가져오세요
}


// ───── 문제 13 ───── (개념05, 응용)
// 오류를 다듬는 함수를 만드세요.
//   23505 → { 종류: "중복", status: 409 }
//   23514 → { 종류: "값오류", status: 400 }
//   42501 → { 종류: "권한없음", status: 403 }
//   status 0 → { 종류: "연결실패", status: 503 }
//   그 밖 → { 종류: "알수없음", status: 500 }
//
// 기대 출력:
// 23505 → 중복/409
// 23514 → 값오류/400
// 42501 → 권한없음/403
// (연결실패) → 연결실패/503
// 42P01 → 알수없음/500

function 오류다듬기(오류, 상태) {
  // TODO: { 종류, status } 를 돌려주세요
}

function 문제13() {
  const 경우들 = [
    ["23505", { code: "23505" }, 409],
    ["23514", { code: "23514" }, 400],
    ["42501", { code: "42501" }, 403],
    ["(연결실패)", { code: "" }, 0],
    ["42P01", { code: "42P01" }, 404],
  ];

  for (const [라벨, 오류, 상태] of 경우들) {
    const 결과 = 오류다듬기(오류, 상태);
    console.log(`${라벨} → ${결과.종류}/${결과.status}`);
  }
}


// ───── 문제 14 ───── (개념05, 응용)
// 저장소의 하나(id) 를 만드세요.
//   못 찾으면 null
//   오류가 오면 던지기
//
// 기대 출력:
// 용접로봇 1호
// null
// 던져진 오류 code: 42501

function 저장소만들기(sb) {
  // TODO: { 하나 } 를 돌려주세요
}

async function 문제14() {
  const { 클라이언트: sb } = 표있는클라이언트(처음자료);
  const 저장소 = 저장소만들기(sb);

  console.log((await 저장소.하나(1)).name);
  console.log(await 저장소.하나(9999));

  // TODO: 오류가 오는 sb 를 넣어서 던져지는지도 확인해 보세요
}


// ───── 문제 15 ───── (개념05, [도전])
// 04단원에서 만든 저장소검사를 통과하는 Supabase 저장소를 만드세요.
// 설비저장소.js 를 보지 말고 먼저 해 보세요.
//
// 기대 출력:
// 통과: 5개, 실패: []

async function 저장소검사(저장소) {
  const 실패 = [];
  let 통과 = 0;
  const 확인 = (이름, 조건) => { if (조건) 통과 += 1; else 실패.push(이름); };

  확인("내보내는함수", ["전부", "하나", "추가", "수정", "삭제"]
    .every((이름) => typeof 저장소[이름] === "function"));
  확인("없으면null", (await 저장소.하나(999999)) === null);
  확인("삭제false", (await 저장소.삭제(999999)) === false);

  const 시험용 = await 저장소.추가({ name: "검사용 설비", line: "A" });
  확인("id고정", (await 저장소.수정(시험용.id, { id: 888888, line: "B" })).id === 시험용.id);

  const 고친것 = await 저장소.수정(시험용.id, { line: "C" });
  확인("칸유지", 고친것.name === 시험용.name && 고친것.status === 시험용.status);

  await 저장소.삭제(시험용.id);
  return { 통과, 실패 };
}

function 내저장소(sb) {
  // TODO: 전부, 하나, 추가, 수정, 삭제 를 만드세요
}


// ============================================================
// 실행 (TODO 를 채우면서 주석을 하나씩 푸세요)
// ============================================================

async function 실행() {
  const { 클라이언트: sb } = 표있는클라이언트(처음자료, { 겹치면안되는칸: "name", 기본값: { status: "정지" } });

  // await 문제1(sb);
  // await 문제2();
  // await 문제3(sb);
  // await 문제4(sb);
  // await 문제5(sb);
  // await 문제6(sb);
  // await 문제7(sb);
  // await 문제8(sb);
  // 문제9();
  // await 문제10();
  // await 문제11();
  // await 문제12();
  // 문제13();
  // await 문제14();
  // await 문제15();
}

실행();


// ============================================================
// SQL 로 써 볼 것 — Supabase SQL Editor 에서
// ============================================================
//
// ★ 아래는 코드로 확인할 수 없습니다. 프로젝트를 만들었으면 직접 쳐 보세요.
//   답은 연습문제_정답.js 맨 아래에 있습니다.
//
// ── S1 ──
//   equipments 표를 Postgres 문법으로 만드세요.
//     id          자동 번호
//     name        글자, 비면 안 되고 겹치면 안 됨
//     line        A/B/C 중 하나
//     status      가동/정지/점검중 중 하나, 기본값 정지
//     temperature 실수, 비어도 됨
//     created_at  시각, 기본값 지금
//
// ── S2 ──
//   그 표에 RLS 를 켜세요.
//   그리고 "로그인한 사람은 읽기, 자기가 만든 것만 고치기" 정책을 쓰세요.
//   (owner_id uuid 칸을 추가해야 합니다)
//
// ── S3 ──
//   라인별로 설비 이름을 모아서 보여 주는 조회를 쓰세요.
//   설비가 없는 라인도 나와야 합니다. (lines 표가 따로 있다고 가정)
//
// ── S4 ──
//   점검을 기록하면서 결과가 '이상' 이면 설비 상태를 '점검중' 으로 바꾸는
//   함수를 만드세요. 둘 다 되거나 둘 다 안 되어야 합니다.
//   그리고 클라이언트에서 부르는 코드도 쓰세요.
//
// ── S5 ──
//   RLS 가 정말 막는지 curl 로 확인하는 명령을 쓰세요.
