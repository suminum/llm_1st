// ============================================================
// 09단원 연습문제 — 구조분해와 스프레드
// ------------------------------------------------------------
// 실행: node 연습문제.js
// ============================================================
//
// TODO 자리에 코드를 쓰고, '기대 출력'과 같은지 확인하세요.
// 1~17은 기본, 18은 응용, 19는 [도전], 20은 에러 확인입니다.

// ───── 문제 1 ───── (개념01 배열 구조분해)
// 아래 배열에서 첫 값과 두 번째 값을 구조분해로 꺼내 한 줄에 출력하세요.

const colors1 = ["빨강", "파랑", "노랑"];

// 기대 출력:
// 빨강 파랑

// TODO: 여기에 코드를 쓰세요
console.log((([first, second] = colors1), first + second));

// ───── 문제 2 ───── (개념01 기본값)
// 아래 배열에서 두 값을 꺼내되, 두 번째 값이 없으면 "없음" 이 나오게 하세요.

const one2 = ["사과"];
const [a, b] = one2;
console.log(a, b ?? "없음");
// 기대 출력:
// 사과 없음

// TODO: 여기에 코드를 쓰세요

// ───── 문제 3 ───── (개념01 나머지)
// 아래 배열에서 첫 값과 나머지를 나눠 차례로 출력하세요.

const nums3 = [1, 2, 3, 4];

// 기대 출력:
// 1
// [ 2, 3, 4 ]
const [top, ...others] = nums3;
console.log(top);
console.log(others);

// TODO: 여기에 코드를 쓰세요

// ───── 문제 4 ───── (개념01 값 교환)
// 아래 두 변수의 값을 구조분해로 맞바꾼 뒤 출력하세요. 임시 변수를 쓰지 마세요.

let left4 = "왼쪽";
let right4 = "오른쪽";

// 기대 출력:
// 오른쪽 왼쪽

// TODO: 여기에 코드를 쓰세요
[left4, right4] = [right4, left4];
console.log(left4, right4);
// ───── 문제 5 ───── (개념01 split 과 함께)
// 아래 날짜 문자열을 "-" 로 나눈 뒤 구조분해로 받아 아래처럼 출력하세요.

const dateText5 = "2026-08-11";

// 기대 출력:
// 2026년 08월 11일

// TODO: 여기에 코드를 쓰세요
const list = dateText5.split("-");
console.log(list[0] + "년" + list[1] + "월" + list[2] + "일");
// ───── 문제 6 ───── (개념02 객체 구조분해)
// 아래 객체에서 title 과 price 를 구조분해로 꺼내 한 줄에 출력하세요.

const book6 = { title: "입문서", price: 25000, author: "김작가" };

// 기대 출력:
// 입문서 25000

// TODO: 여기에 코드를 쓰세요
const { title, author } = book6;
console.log(title, author);
// ───── 문제 7 ───── (개념02 이름 바꾸기)
// book6 의 author 를 writer 라는 이름으로 꺼내 출력하세요.
//
// 기대 출력:
// 김작가

// TODO: 여기에 코드를 쓰세요

// ───── 문제 8 ───── (개념02 기본값)
// 아래 객체에서 theme 과 fontSize 를 꺼내되,
// fontSize 가 없으면 14 가 나오게 하세요.

const config8 = { theme: "dark" };

// 기대 출력:
// dark 14

// TODO: 여기에 코드를 쓰세요
const { theme, fontSize = 14 } = config8;

console.log(theme, fontSize);
// ───── 문제 9 ───── (개념02 나머지)
// 아래 객체에서 password 만 빼고 나머지를 모아 출력하세요.

const account9 = { userId: "abc", password: "1234", nickname: "서연" };

// 기대 출력:
// { userId: 'abc', nickname: '서연' }

// TODO: 여기에 코드를 쓰세요
const { password, ...profile } = account9;
console.log(profile);
// ───── 문제 10 ───── (개념02 매개변수 구조분해)
// { name, city } 를 받아 "이름은 도시 거주" 를 출력하는 함수를 만들고,
// 아래 객체로 호출하세요. 함수 안에서 user10. 을 붙이면 안 됩니다.

const user10 = { name: "김민준", age: 20, city: "부산" };

// 기대 출력:
// 김민준은 부산 거주
function introduce({ name, city }) {
  console.log(name + city + "거주");
}
introduce(user10);
//왜 여기서 객체를 넘길까 인자가 다른형태인데시발
// TODO: 여기에 코드를 쓰세요

// ───── 문제 11 ───── (개념02 콜백 구조분해)
// 아래 배열을 map 으로 돌면서 이름만 뽑아 출력하세요.
// 콜백의 매개변수에서 구조분해를 쓰세요.

const products11 = [
  { name: "아메리카노", price: 4000 },
  { name: "케이크", price: 6000 },
];
console.log(products11.map(({ name }) => name));
// 기대 출력:
// [ '아메리카노', '케이크' ]

// TODO: 여기에 코드를 쓰세요

// ───── 문제 12 ───── (개념03 스프레드 복사)
// 아래 배열을 복사한 뒤 복사본만 오름차순 정렬해 출력하고,
// 원본이 그대로인지 확인하기 위해 원본도 출력하세요.

const scores12 = [30, 10, 20];

// 기대 출력:
// [ 10, 20, 30 ]
// [ 30, 10, 20 ]

// TODO: 여기에 코드를 쓰세요
let copy = [...scores12];
copy.sort((a, b) => a - b);
console.log(copy);
console.log(scores12);
// ───── 문제 13 ───── (개념03 합치기)
// 아래 두 배열을 스프레드로 합쳐 출력하세요. concat 을 쓰지 마세요.

const a13 = [1, 2];
const b13 = [3, 4];

// 기대 출력:
// [ 1, 2, 3, 4 ]

// TODO: 여기에 코드를 쓰세요
console.log([...a13, ...b13]);
// ───── 문제 14 ───── (개념03 함수 인자로 펼치기)
// 아래 배열의 최댓값을 Math.max 로 구해 출력하세요.

const temps14 = [45, 88, 62];

// 기대 출력:
// 88
console.log(Math.max(...temps14));
// TODO: 여기에 코드를 쓰세요

// ───── 문제 15 ───── (개념04 객체 수정)
// 아래 객체의 age 만 30 으로 바꾼 '새 객체' 를 만들어 출력하고,
// 원본도 출력하세요. 원본은 바뀌면 안 됩니다.

const base15 = { name: "박지훈", age: 28 };

// 기대 출력:
// { name: '박지훈', age: 30 }
// { name: '박지훈', age: 28 }

// TODO: 여기에 코드를 쓰세요
let newbase = { ...base15, age: 30 };
console.log(newbase);
console.log(base15);

// ───cons── 문제 16 ───── (개념04 병합)
// 기본 설정 위에 사용자 설정을 덮어쓴 결과를 출력하세요.
// 사용자가 정한 값이 이겨야 합니다.

const defaults16 = { theme: "light", fontSize: 14 };
const userSetting16 = { theme: "dark" };

// 기대 출력:
// { theme: 'dark', fontSize: 14 }
let setting = { ...defaults16, ...userSetting16 };
console.log(setting);
// TODO: 여기에 코드를 쓰세요

// ───── 문제 17 ───── (개념05 나머지 매개변수)
// 넘어온 숫자를 모두 더해서 돌려주는 sumAll 함수를 만드세요.
// 인자 개수는 몇 개든 됩니다.
//
// 기대 출력:
// 15
// 3
// 0

// TODO: 여기에 코드를 쓰세요
// (sumAll(1,2,3,4,5) / sumAll(1,2) / sumAll() 을 차례로 출력)

// ───── 문제 18 ───── [응용]
// 아래 문자열을 뒤집어 출력하세요. 스프레드를 쓰세요.

const word18 = "자바스크립트";

// 기대 출력:
// 트립크스바자

// TODO: 여기에 코드를 쓰세요

// ───── 문제 19 ───── [도전]
// 아래 할 일 목록에서 id 가 2인 항목만 done 을 true 로 바꾼 '새 배열' 을 만들고,
// 그 결과를 "제목 완료/미완료" 형태로 출력하세요.
// 원본 todos19 는 바뀌면 안 됩니다.
// 이 단원에서 가장 어렵습니다. 배열 복사와 객체 복사를 한 번에 써야 합니다.

const todos19 = [
  { id: 1, text: "장보기", done: false },
  { id: 2, text: "청소", done: false },
];

// 힌트: 세 가지를 이어서 씁니다.
//       ① map 으로 새 배열을 만든다 (원본은 그대로 남습니다)
//       ② 각 항목에서 id 가 2인 것만 { ...todo, done: true } 로 바꾼다
//       ③ 나머지는 그대로 돌려준다 — 삼항 연산자를 쓰면 한 줄입니다
//       새 객체를 안 만들고 todo.done = true 로 고치면 원본이 같이 바뀝니다.
//
// 기대 출력:
// 장보기 미완료
// 청소 완료
// 원본 확인: false

// TODO: 여기에 코드를 쓰세요
// (마지막 줄은 console.log("원본 확인:", todos19[1].done) 로 확인)

// ───── 문제 20 ───── (에러 확인 — 맨 마지막)
// 아래 줄의 주석을 풀고 실행해서 어떤 에러가 나는지 확인하세요.
// 확인했으면 반드시 다시 주석 처리하세요.

// const { value } = null;

// 무슨 에러가 났나요? 값이 있을지 없을지 모를 때는 어떻게 하면 될까요?
// 답: __________________________________________
