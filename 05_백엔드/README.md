# 백엔드 자료

Node.js 와 Express 로 **서버를 처음부터 만드는** 교재입니다.
12단원 · 155파일 · 약 35,700줄.

프로그래밍이 처음은 아니지만 **백엔드는 처음인** 사람을 대상으로 합니다.
JS 기초(변수·함수·배열·객체·비동기·DOM)를 알고 있다고 보고 시작합니다.

---

## 시작하기

```bash
cd 백엔드자료
npm install          # 한 번만
node 01_Node_시작하기/개념01_Node란_무엇인가.js
```

패키지는 **맨 위 폴더에 한 번만** 설치합니다.
Node 가 `node_modules` 를 위쪽으로 거슬러 올라가며 찾기 때문에,
단원 폴더 안에서 실행해도 잘 찾습니다.

| 도구 | 왜 |
| --- | --- |
| Node 18 이상 | `fetch` 가 내장돼 있어야 합니다 (24 권장) |
| VS Code | 아무 에디터나 됩니다 |
| **Postman** | 03단원부터 필수입니다. 브라우저로는 POST 를 못 보냅니다 |
| 크롬 | 08·10단원에서 F12 를 계속 씁니다 |

---

## 단원

| 단원 | 무엇을 | 끝나면 |
| --- | --- | --- |
| **00** 제조 현장 이해 | 스마트팩토리·현장 문서 6종·값 등급 | ★ 무엇을 만드는지 안다 |
| **01** Node 시작하기 | Node·모듈·npm·에러 읽기 | 터미널에서 JS 를 돌린다 |
| **02** 파일 다루기 | path·fs·동기와 비동기·JSON 저장 | 파일을 읽고 쓴다 |
| **03** HTTP와 서버 | 요청/응답·상태코드·맨손으로 서버 | `http` 만으로 API 를 만든다 |
| **04** Express 시작하기 | 라우팅·요청 읽기·응답·에러·라우터 분리 | 03단원 200줄이 100줄이 된다 |
| **05** 미들웨어 | next·인증·권한·검증·morgan | 라우트에서 검사 코드가 사라진다 |
| **06** REST API 설계 | 자원·페이징·검색·응답 형식·문서 | 문서를 보고 API 를 만든다 |
| **07** 데이터 저장하기 | 파일 저장·계층 나누기·동시성 | 껐다 켜도 남는다 |
| **08** 정적파일과 CORS | static·같은 출처 정책·프록시 | 브라우저가 왜 막는지 안다 |
| **09** 파일 업로드 | multipart·multer·제한·고아 파일 | 사진을 올리고 내려받는다 |
| **10** 프론트엔드 연동 | fetch·로딩/에러·폼·목록·업로드 화면 | 화면과 서버를 붙인다 |
| | ↑ 화면은 순수 HTML/JS 입니다. React 쪽은 `React자료` 14단원(서버와 연동)에 있습니다 | |
| **11** 종합 실습 | 전부 합쳐 프로젝트 하나 | 혼자 서버를 만든다 |

### 왜 00단원이 먼저인가

코드부터 시작하면 "이 기능이 왜 필요하지?" 를 스스로 답할 수 없습니다.

00단원은 **코드를 거의 안 씁니다.** 순수 JS 배열·객체만 씁니다.
대신 이런 걸 정합니다.

- 현장 문서가 어떻게 생겼나 (표형 · 서식형 · 덧붙임형)
- ★ 어떤 값이 **밖으로 나가면 안 되나** (이름·사번·연락처)
- ★★ AI 에게 **시키면 안 되는 것**은 무엇인가 (세기·판정·실행·법적 판단)

`data/샘플문서/` 의 여섯 개가 **PART 3·4·5·6 전체의 소재**입니다.
09단원 업로드 실습, PART 5 문서 Q&A, PART 6 프로젝트가 이 파일들을 그대로 씁니다.

### 왜 03단원에서 맨손으로 만드나

Express 를 바로 배우면 "왜 이렇게 쓰는지" 를 모릅니다.
`if` 를 서른 개 쓰고 `writeHead` 를 반복해 본 사람만
04단원에서 `app.get` 이 왜 고마운지 압니다.

### 왜 07단원에서 파일로 저장하나

데이터베이스는 PART 4 입니다.
그 전에 **왜 데이터베이스가 필요한지** 를 손으로 겪습니다.
07단원에서 20건을 동시에 저장하면 19건이 사라지는 것을 직접 재 봅니다.

---

## 파일 이름 규칙

```
개념01_....js       읽고 실행하며 배우는 것
연습문제.js          서버 없이 푸는 문제 (TODO 를 채웁니다)
연습문제_정답.js
연습문제_서버.js      서버를 직접 만드는 문제
연습문제_서버_정답.js
```

**정답을 먼저 보지 마세요.** 설명은 정답 파일에 더 많지만,
막힌 뒤에 읽어야 남습니다.

---

## 이 자료의 약속

### 적힌 출력은 전부 진짜입니다

```js
console.log(1 + 2);
// 출력: 3
```

이 주석은 손으로 적은 것이 아니라 **기계로 대조한 것**입니다.
서버 자료도 마찬가지입니다.

```js
// 확인: GET /health
// 응답: 200 {"status":"ok"}
```

이건 진짜로 서버를 띄워 요청을 보내고 응답을 맞춰 본 것입니다.
화면도 진짜 크롬으로 버튼을 눌러 확인했습니다.

> **결과가 다르면 여러분이 틀린 것입니다.** 자료를 의심하지 말고 코드를 보세요.
> 그래도 다르면 알려 주세요. 자료가 틀렸을 수 있습니다.

### 틀린 예를 일부러 넣어 뒀습니다

`자주 하는 실수` 절에 있는 것들은 대부분 **자료를 만들면서 실제로 겪은** 것입니다.
`✏️ 직접 해보기` 에 "일부러 망가뜨려 보세요" 가 자주 나옵니다. 꼭 해 보세요.
**확인한 뒤에는 반드시 되돌리세요.**

### 한글은 값에만 씁니다

이 자료가 계속 강조하는 규칙입니다.

| 곳 | 한글 | 안 쓰면 무슨 일이 |
| --- | --- | --- |
| 라우트 주소 | ✗ | 에러 없이 조용히 404 |
| 응답 헤더 값 | ✗ | 500 으로 터짐 |
| 요청 헤더 값 | ✗ | 요청이 아예 안 나감 |
| 업로드 필드 이름 | ✗ | `Unexpected field` |
| 업로드 파일 이름 | △ | 깨져서 도착 (되돌려야 함) |
| 쿼리·경로·본문·응답 | ○ | — |

안 되는 것은 전부 **헤더에 실려 가는 것**입니다.
HTTP 헤더는 아주 오래된 약속이라 영어와 숫자만 담깁니다.

---

## 검증

"다 됐다" 라고 말하기 전에 돌립니다. 사람 눈으로는 안 잡힙니다.

```bash
npm run 검증          # 출력 · 서버 요청 · 화면 · 정합성
npm run 검증:브라우저  # CORS (사람이 읽고 판단)
```

| 트랙 | 규모 |
| --- | --- |
| 출력 대조 | 파일 33개 |
| 서버 요청 대조 | 서버 47개 / **요청 553건** |
| 화면 대조 (진짜 크롬) | **55건** |
| 정합성 검사 | 파일 129개 |

자세한 사용법은 [`_검증도구/README.md`](_검증도구/README.md) 에 있습니다.

---

## 다음은

이 자료가 끝나면 PART 4 로 갑니다.

| 무엇 | 어디를 고치나 |
| --- | --- |
| 데이터베이스 (SQL · Supabase) | `repositories/` 만 |
| 환경변수 · 배포 (EC2 · PM2) | `server.js` 주변 |
| S3 파일 저장 | `services/사진서비스.js` 만 |

07단원에서 계층을 나눈 이유가 이것입니다.
저장 방법이 바뀌어도 **주소·업무 규칙·응답 형식은 그대로**입니다.

---

## 시리즈 지도

| PART | 폴더 | 내용 |
| --- | --- | --- |
| 1 | `HTMLCSS자료` | HTML·CSS — 자바스크립트가 안 나옵니다 (15단원) |
| 1 | `JS자료` | 자바스크립트 기초 — 변수부터 fetch 까지 (15단원) |
| 2 | `React자료` | React·Vite·Router — 화면 만들기 (16단원) |
| 3 | **`백엔드자료`** | Node·Express·REST·미들웨어·업로드 (12단원)  ← **이 자료** |
| 4 | `클라우드자료` | SQL·Supabase·EC2·PM2·S3·배포·로그인 (11단원) |
| 5 | `AI자료` | LLM·프롬프트·스트리밍·문서 QA (7단원) |
| 6 | `(준비 중)` | 통합 프로젝트 — 위의 것을 하나로 묶습니다 |
| 선택 | `TS자료` | 타입스크립트 — PART 2 뒤 언제든 (9단원) |
| 선택 | `DB자료` | 데이터베이스 심화 — 단독으로도 완결됩니다 (10단원) |

PART 1~6 은 순서대로 봅니다. **선택** 두 개는 아무 때나 봐도 되고, 안 봐도 됩니다.

- `TS자료` 는 React자료 15단원(부록)을 더 깊게 다룬 것입니다.
- `DB자료` 는 클라우드자료 02~03단원의 SQL 을 데이터베이스 쪽에서 깊게 다시 봅니다.
  둘 다 봐도 되고, 하나만 봐도 됩니다.
