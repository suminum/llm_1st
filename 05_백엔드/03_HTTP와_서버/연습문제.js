// ============================================================
// 03단원 연습문제 — HTTP 와 주소 다루기
// ------------------------------------------------------------
// 실행: node 연습문제.js
// ============================================================
//
// 이 파일은 '서버 없이' 푸는 문제들입니다.
// 서버를 직접 만드는 문제는 연습문제_서버.js 에 따로 있습니다.
//
// 규칙
//   · 위에서부터 순서대로 푸세요. 아래 문제가 위 문제의 답을 씁니다.
//   · 한 문제 풀 때마다 저장하고 실행해서 결과를 확인하세요.
//   · '기대 출력' 과 다르면 다음으로 넘어가지 마세요.
//   · 막히면 개념 파일을 다시 여세요. 정답을 먼저 보지 마세요.


// ───── 문제 1 ───── 주소 뜯어보기
// 아래 주소에서 경로 / 호스트 / 포트를 각각 출력하세요.
// 포트가 숫자인지 글자인지도 typeof 로 확인해 보세요.
//
// 힌트: new URL(글자) 로 만들면 pathname, host, port 를 꺼낼 수 있습니다. (개념01)

const 주소글자 = "http://localhost:3000/api/documents?page=2&size=10";

// 기대 출력:
// /api/documents
// localhost:3000
// 3000
// string

// TODO: 여기에 코드를 쓰세요


// ───── 문제 2 ───── 쿼리 꺼내기
// 문제 1의 주소에서
//   ① page 값
//   ② sort 값 (없습니다)
//   ③ sort 가 있는지 없는지
// 를 각각 출력하세요.
//
// 힌트: searchParams.get / searchParams.has

// 기대 출력:
// 2
// null
// false

// TODO: 여기에 코드를 쓰세요


// ───── 문제 3 ───── 페이지 번호를 안전하게 꺼내기
// URL 을 받아서 page 값을 '숫자로' 돌려주는 함수를 만드세요.
// 이름은 페이지번호 로 하세요.
//
// 조건:
//   - page 가 없으면 1
//   - 숫자가 아니면 1
//   - 1보다 작으면 1
//
// 힌트: Number 로 바꾸고, Number.isInteger 로 확인합니다.
//
// ★ 함정이 하나 숨어 있습니다.
//   page 가 없을 때 get 은 null 을 주는데, Number(null) 은 NaN 이 아니라 0 입니다.
//   그래서 "숫자인가" 만 확인하면 통과해 버립니다. 범위까지 확인해야 합니다.

// 기대 출력:
// 3
// 1
// 1
// 1
//
// (아래 네 줄로 시험해 보세요)
//   페이지번호(new URL("http://localhost:3000/x?page=3"))
//   페이지번호(new URL("http://localhost:3000/x"))
//   페이지번호(new URL("http://localhost:3000/x?page=abc"))
//   페이지번호(new URL("http://localhost:3000/x?page=0"))

// TODO: 여기에 코드를 쓰세요


// ───── 문제 4 ───── 같은 이름의 쿼리가 여러 개일 때
// 아래 주소에서 tag 를 꺼내 보세요.
//   ① get 으로 꺼낸 것
//   ② getAll 로 꺼낸 것
//   ③ 없는 이름을 getAll 로 꺼낸 것
//
// 셋의 차이를 눈으로 보는 게 목적입니다.

const 주소4 = new URL("http://localhost:3000/search?tag=안전&tag=품질&q=점검");

// 기대 출력:
// 안전
// [ '안전', '품질' ]
// []

// TODO: 여기에 코드를 쓰세요


// ───── 문제 5 ───── 상황에 맞는 상태코드 고르기
// 아래 여덟 가지 상황에 어떤 상태코드를 돌려줘야 할까요?
// 상황과 코드를 짝지어 출력하세요.
//
// 힌트: 객체에 { "상황": 코드 } 로 적고, Object.keys 로 돌면서 찍으면 됩니다.
//       화살표는 → 입니다. 복사해서 쓰세요.
//
// 쓸 수 있는 코드: 200 201 204 400 401 403 404 500

// 기대 출력:
// 목록을 잘 돌려줌 → 200
// 새 문서를 만듦 → 201
// 지웠고 돌려줄 내용이 없음 → 204
// title 을 안 보냄 → 400
// 로그인을 안 함 → 401
// 로그인은 했지만 권한이 없음 → 403
// 그 번호의 문서가 없음 → 404
// 내 코드에서 TypeError 가 남 → 500

// TODO: 여기에 코드를 쓰세요


// ───── 문제 6 ───── 상태코드를 무리로 나누기
// 상태코드를 받아서 어느 무리인지 글자로 돌려주는 함수 분류 를 만드세요.
//
//   200~299  →  "성공"
//   300~399  →  "다른 곳으로"
//   400~499  →  "보낸 쪽 잘못"
//   500 이상 →  "서버 잘못"
//   그 밖    →  "알 수 없음"
//
// 만든 뒤 [200, 204, 301, 404, 500] 을 map 으로 돌려
// " / " 로 이어서 한 줄로 출력하세요.

// 기대 출력:
// 성공 / 성공 / 다른 곳으로 / 보낸 쪽 잘못 / 서버 잘못

// TODO: 여기에 코드를 쓰세요


// ───── 문제 7 ───── 경로를 조각내기
// 경로 글자를 받아 '/' 로 나눈 조각 배열을 돌려주는 함수 조각내기 를 만드세요.
// 빈 조각은 버려야 합니다.
//
// 힌트: split("/") 한 뒤 filter 로 빈 글자를 버립니다.
//       filter(Boolean) 이라고 쓰면 됩니다.

// 기대 출력:
// [ 'documents' ]
// [ 'documents', '12' ]
// [ 'documents', '12', 'files' ]
// []
//
// (시험할 값: "/documents", "/documents/12", "/documents/12/files", "/")

// TODO: 여기에 코드를 쓰세요


// ───── 문제 8 ───── 경로를 분석하는 함수
// 경로를 받아 { resource, id } 모양의 객체를 돌려주는 함수 경로분석 을 만드세요.
//
//   - resource 는 첫 조각. 없으면 null
//   - id 는 두 번째 조각을 '숫자로' 바꾼 것. 없으면 null
//
// 힌트: 문제 7의 조각내기를 그대로 쓰면 됩니다.
//       ?? 는 "왼쪽이 없으면 오른쪽" 입니다. (JS자료 09단원)
//
// ★ 속성 이름은 영어로 쓰세요. 한글로 쓰면 출력에 따옴표가 붙어 다르게 보입니다.

// 기대 출력:
// { resource: 'documents', id: null }
// { resource: 'documents', id: 12 }
// { resource: null, id: null }
//
// (id 가 12 로 따옴표 없이 나와야 합니다. '12' 로 나오면 Number 를 안 한 것입니다)

// TODO: 여기에 코드를 쓰세요


// ───── 문제 9 ───── 헤더는 소문자입니다
// 아래 headers 객체에서
//   ① "Content-Type" 으로 찾은 값
//   ② "content-type" 으로 찾은 값
// 을 출력해 차이를 보세요.
//
// 그다음, 대소문자를 아무렇게나 줘도 찾아 주는 함수 헤더값(headers, 이름) 을 만드세요.

const headers = {
  host: "localhost:3000",
  "content-type": "application/json; charset=utf-8",
  "user-agent": "PostmanRuntime/7.36.0",
};

// 기대 출력:
// undefined
// application/json; charset=utf-8
// application/json; charset=utf-8
// PostmanRuntime/7.36.0
//
// (셋째 줄은 헤더값(headers, "Content-Type"),
//  넷째 줄은 헤더값(headers, "USER-AGENT") 입니다)

// TODO: 여기에 코드를 쓰세요


// ───── 문제 10 ───── 본문이 JSON 인지 확인하기
// headers 를 받아 본문이 JSON 인지 true/false 로 돌려주는 함수
// JSON본문인가 를 만드세요.
//
// ★ 두 가지를 조심하세요.
//   ① 실제로 오는 값은 "application/json; charset=utf-8" 입니다.
//      === 로 비교하면 false 가 됩니다. 들어 있는지를 봐야 합니다.
//   ② 헤더가 아예 없을 수도 있습니다. 이때 터지지 않아야 합니다.

// 기대 출력:
// true
// true
// false
// false
//
// (시험할 값)
//   { "content-type": "application/json; charset=utf-8" }
//   { "content-type": "application/json" }
//   { "content-type": "text/plain" }
//   {}

// TODO: 여기에 코드를 쓰세요


// ───── 문제 11 ───── 빠진 값 찾기
// 데이터와 '반드시 있어야 할 이름들' 을 받아,
// 빠진 이름만 배열로 돌려주는 함수 빠진값찾기(데이터, 필수들) 를 만드세요.
//
// 그다음 그 결과로 상태코드를 정해 출력하세요.
//   빠진 게 없으면  → 201
//   빠진 게 있으면  → 400 과 함께 빠진 이름들을 쉼표로 이어서

// 기대 출력:
// [ 'type', 'author' ]
// []
// 400 type 을(를) 넣어 주세요
//
// (시험할 값)
//   빠진값찾기({ title: "작업표준서" }, ["title", "type", "author"])
//   빠진값찾기({ title: "a", type: "b", author: "c" }, ["title", "type", "author"])
//   빠진값찾기({ title: "작업표준서" }, ["title", "type"])  → 이걸로 상태코드 결정

// TODO: 여기에 코드를 쓰세요


// ───── 문제 12 ───── 주소 설계 고치기
// 아래는 잘못 설계된 주소들입니다. REST 규칙에 맞게 고치세요.
// 고친 것을 배열에 담고 한 줄씩 출력하면 됩니다.
//
//   ① GET /getDocuments              문서 목록 보기
//   ② GET /createDocument            문서 새로 만들기
//   ③ GET /getDocument?id=3          3번 문서 하나 보기
//   ④ GET /deleteDocument?id=3       3번 문서 지우기
//   ⑤ GET /getDocumentsByType?type=표준   표준 종류만 보기
//
// 규칙
//   · 주소에 동사를 쓰지 않습니다. 무엇을 할지는 메서드가 말합니다.
//   · 자원 이름은 복수형 명사입니다. (documents)
//   · 하나를 콕 집을 때는 경로에, 걸러 볼 때는 쿼리에 씁니다.

// 기대 출력:
// GET /documents
// POST /documents
// GET /documents/3
// DELETE /documents/3
// GET /documents?type=표준

// TODO: 여기에 코드를 쓰세요


// ───── 문제 13 ───── 이 코드는 왜 안 될까요
// 아래 두 줄의 주석을 지우고 실행하면 에러가 납니다.
// 실행해서 에러를 직접 본 다음, 아래 빈칸을 채우세요.

// const 나쁜주소 = new URL("/documents?page=2");
// console.log(나쁜주소.pathname);

// 에러 이름: ______________________________
//
// 왜 이런 에러가 났나:
//   __________________________________________
//
// 어떻게 고치나 (코드로):
//   __________________________________________
//
// 서버 코드에서는 왜 이 문제가 늘 생기나:
//   __________________________________________
//   (힌트: req.url 에는 무엇이 들어 있었나요? 개념03 섹션 2를 다시 보세요)

// 고친 코드를 아래에 쓰고 실행해 보세요.
//
// 기대 출력:
// /documents
// http://localhost:3000/documents?page=2

// TODO: 여기에 코드를 쓰세요
