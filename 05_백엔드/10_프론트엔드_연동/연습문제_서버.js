// ============================================================
// 10단원 서버 연습문제 — 설비 관리 화면 하나 완성하기
// ------------------------------------------------------------
// 실행: npx nodemon 연습문제_서버.js
//       브라우저에서 http://localhost:3000 을 여세요.
// ============================================================
//
// ★ 이 문제의 목적
//   개념01~04 에서 배운 것을 '한 화면' 에 전부 모으는 것입니다.
//
//   ★ 서버는 미리 만들어 두었습니다. 아래 코드는 고치지 마세요.
//     여러분이 만들 것은 화면 하나입니다.
//
//       public/연습_관리화면.html   ← 이 파일을 만드세요
//
//   ★★ 그 자리에 완성본이 이미 들어 있습니다.
//     직접 만들려면 먼저 public/연습_관리화면.html 을 지우거나
//     다른 이름(예: 연습_관리화면_완성본.html)으로 바꿔 두고 시작하세요.
//     그대로 두면 서버를 켜자마자 완성된 화면이 떠서 연습이 되지 않습니다.
//
//   등록 폼(이름 · 라인)과 목록(검색 · 라인 거르기 · 페이징 · 삭제)이 있는 화면입니다.
//   아래 일곱 문제가 그 화면을 나눠 놓은 것입니다. 순서대로 하면 완성됩니다.
//
//   확인은 브라우저로 합니다. http://localhost:3000
//
//
// ───── 문제 1 ───── 화면 뼈대와 api.js 쓰기
// 등록 폼과 목록이 들어갈 자리를 만들고, 목록을 한 번 그려 보세요.
//
// ★ fetch 를 직접 쓰지 마세요. public/api.js 의 함수만 부릅니다.
//   화면마다 fetch 를 흩어 놓으면 규칙이 바뀔 때 전부 고쳐야 합니다(개념01).
//
// 눈으로 확인: 화면을 열면 설비 네 건이 표로 보인다.
//
//
// ───── 문제 2 ───── 네 가지 상태 전부 그리기
// 로딩 중 · 성공 · 빈 결과 · 실패 넷을 각각 다르게 보여 주세요(개념02).
//
//   빈 결과는 에러가 아닙니다. "없습니다" 를 보여 줍니다.
//   실패는 콘솔이 아니라 화면에 띄우고, 다시 시도할 버튼도 같이 줍니다.
//
// 눈으로 확인: 검색어에 "ㅁㄴㅇㄹ" 을 넣으면 빈 결과 화면이,
//             서버를 끄고 새로고침하면 실패 화면이 나온다.
//
//
// ───── 문제 3 ───── 검색과 라인 거르기
// 검색어 칸과 라인 선택을 붙이세요.
//
//   □ 빈 값은 주소에 넣지 않습니다. ?q=&line= 이 붙으면 안 됩니다.
//   □ 조건이 바뀌면 쪽 번호를 1로 되돌립니다.   ← 제일 많이 빠뜨립니다
//
// 눈으로 확인: 3쪽을 보다가 검색어를 넣으면 1쪽으로 돌아간다.
//             F12 → Network 에서 주소에 빈 값이 안 붙는다.
//
//
// ───── 문제 4 ───── 페이징
// 이전·다음 버튼과 "N / M 쪽" 표시를 붙이세요.
//
//   □ total 은 서버가 준 값을 씁니다. 화면에서 세면 지금 쪽 개수만 나옵니다.
//   □ 갈 수 없는 쪽 버튼은 잠급니다. 첫 쪽에서 이전, 끝 쪽에서 다음.
//
// 눈으로 확인: 첫 쪽에서 '이전' 이 눌리지 않는다.
//
//
// ───── 문제 5 ───── 등록 폼
// 이름과 라인을 받아 등록하세요(개념03).
//
//   □ preventDefault 를 부릅니다. 안 부르면 페이지가 새로고침됩니다.
//   □ 보내기 전에 지난 오류 표시를 지웁니다.
//   □ 서버가 준 details 를 칸마다 붙여 보여 줍니다. message 하나만 띄우지 않습니다.
//   □ 보내는 동안 버튼을 잠그고, 푸는 것은 finally 에 둡니다.
//   □ 성공하면 폼을 비우고 목록을 새로 그립니다.
//
// 눈으로 확인: 이름을 비우고 보내면 이름 칸 아래에만 빨간 글씨가 뜬다.
//             고쳐서 다시 보내면 그 글씨가 사라진다.
//
//
// ───── 문제 6 ───── 삭제
// 줄마다 삭제 버튼을 붙이세요.
//
// ★ 버튼마다 addEventListener 를 걸지 마세요. 이벤트 위임을 쓰세요.
//   목록을 다시 그릴 때마다 거는 방식은 그릴 때마다 핸들러가 쌓입니다.
//
// 눈으로 확인: 목록을 여러 번 새로 그린 뒤 삭제해도 한 번만 지워진다.
//
//
// ───── 문제 7 ───── 디바운스와 늦게 온 응답
// 마지막 두 가지를 챙기세요(개념02 섹션4 · 개념04).
//
//   □ 검색은 글자마다 보내지 않고 잠깐 기다렸다 보냅니다(디바운스).
//   □ 늦게 온 응답이 최신 화면을 덮어쓰지 않게 합니다.
//     요청마다 번호를 붙여 두고 마지막 것만 그립니다.
//
// 눈으로 확인: 검색어를 빠르게 치면 요청이 글자 수만큼 나가지 않는다.
//             느린 응답이 뒤늦게 와도 화면이 예전 결과로 돌아가지 않는다.

const express = require("express");
const path = require("path");

const app = express();
const PORT = Number(process.env.PORT) || 3000;

app.use(express.json());
app.use(express.static(path.join(__dirname, "public")));

app.get("/", (req, res) => res.redirect("/연습_관리화면.html"));


// ───── 서버 (여기는 그대로 두세요) ─────

let 설비들 = [
  { id: 1, name: "컨베이어 1호", line: "A", status: "가동" },
  { id: 2, name: "프레스 1호", line: "B", status: "정지" },
  { id: 3, name: "용접로봇 1호", line: "C", status: "가동" },
  { id: 4, name: "검사기 1호", line: "A", status: "점검중" },
];

let 다음번호 = 5;

app.get("/api/v1/equipments", (req, res) => {
  const q = (req.query.q ?? "").trim().toLowerCase();
  const { line } = req.query;

  let 결과 = 설비들;

  if (q) {
    결과 = 결과.filter((설비) =>
      ["name", "line", "status"].some((키) => String(설비[키]).toLowerCase().includes(q))
    );
  }

  if (line) 결과 = 결과.filter((설비) => 설비.line === line);

  const total = 결과.length;

  let page = Number(req.query.page);
  let limit = Number(req.query.limit);
  if (!Number.isInteger(page) || page < 1) page = 1;
  if (!Number.isInteger(limit) || limit < 1) limit = 10;

  const 시작 = (page - 1) * limit;

  res.json({
    data: 결과.slice(시작, 시작 + limit),
    meta: { page, limit, total, totalPages: Math.ceil(total / limit) },
  });
});

app.post("/api/v1/equipments", (req, res) => {
  const { name, line } = req.body || {};
  const 항목 = [];

  if (!name) 항목.push({ 키: "name", 이유: "필수입니다" });
  else if (name.length < 2) 항목.push({ 키: "name", 이유: "2글자 이상이어야 합니다" });

  if (!line) 항목.push({ 키: "line", 이유: "필수입니다" });
  else if (!["A", "B", "C"].includes(line))
    항목.push({ 키: "line", 이유: "A, B, C 중 하나여야 합니다" });

  if (항목.length > 0) {
    return res.status(400).json({
      error: { code: "VALIDATION_FAILED", message: "입력값이 올바르지 않습니다", details: 항목 },
    });
  }

  if (설비들.some((설비) => 설비.name === name)) {
    return res.status(409).json({
      error: { code: "DUPLICATED", message: "이미 있는 설비 이름입니다" },
    });
  }

  const 새설비 = { id: 다음번호, name, line, status: "정지" };
  다음번호 += 1;
  설비들.push(새설비);

  res.status(201).json({ data: 새설비 });
});

app.delete("/api/v1/equipments/:id", (req, res) => {
  const 번호 = Number(req.params.id);
  const 설비 = 설비들.find((설비) => 설비.id === 번호);

  if (!설비) {
    return res.status(404).json({ error: { code: "NOT_FOUND", message: "설비를 찾을 수 없습니다" } });
  }

  // ★ 07단원의 업무 규칙입니다. 화면이 이걸 어떻게 보여 줄지 생각해 보세요.
  if (설비.status === "가동") {
    return res.status(409).json({
      error: { code: "CONFLICT", message: "가동 중인 설비는 삭제할 수 없습니다" },
    });
  }

  설비들 = 설비들.filter((설비) => 설비.id !== 번호);
  res.sendStatus(204);
});


app.use((req, res) => {
  res.status(404).json({ error: { code: "ROUTE_NOT_FOUND", message: "그런 주소가 없습니다" } });
});

app.use((err, req, res, next) => {
  console.error(`[에러] ${req.method} ${req.path} — ${err.message}`);
  res.status(500).json({ error: { code: "INTERNAL_ERROR", message: "서버에서 문제가 생겼습니다" } });
});


app.listen(PORT, () => {
  console.log(`서버가 켜졌습니다.  http://localhost:${PORT}`);
});


// ============================================================
// 화면에 꼭 넣어야 할 id (검증이 이 이름으로 찾습니다)
// ============================================================
//
//   #폼        등록 폼
//   #name      이름 입력칸        (name 속성도 "name" 으로)
//   #line      라인 select        (name 속성도 "line" 으로)
//   #오류-name  이름 칸 오류 자리
//   #오류-line  라인 칸 오류 자리
//   #보내기     등록 버튼
//   #알림       성공·실패 알림 자리
//
//   #검색       검색 입력칸
//   #라인거르기  라인 select
//   #요약       "전체 N건 중 M건" / 로딩 / 빈 결과 안내
//   #본문       표의 tbody
//   #쪽정보     "1 / 2"
//   #이전쪽 #다음쪽  쪽 버튼
//   .지우기     각 줄의 삭제 버튼 (data-id 에 번호)


// ============================================================
// 다 만들었으면 이 순서대로 눌러 보세요
// ============================================================
//
//   1  처음 화면                       3건 + "1 / 2"   (limit 을 3 으로 두세요)
//   2  '다음' 누르기                   2쪽, '다음' 이 잠김
//   3  2쪽에서 '로봇' 검색              1쪽으로 돌아가고 1건  ★★
//   4  '없는것' 검색                    "조건에 맞는 설비가 없습니다"
//   5  빈 폼으로 등록                    두 칸에 빨간 글씨
//   6  라인을 Z 로 등록                  라인 칸에만 빨간 글씨
//   7  제대로 등록                       목록이 새로 그려지고 전체가 늘어남
//   8  같은 이름으로 또 등록              이름 칸에 "이미 있는 설비 이름입니다"
//   9  가동 중인 설비 삭제                "가동 중인 설비는 삭제할 수 없습니다"
//  10  정지 중인 설비 삭제                지워지고 목록이 새로 그려짐
//
// ★★ 3번이 이 실습의 채점 기준입니다.
//   2쪽을 보다가 검색했을 때 1쪽으로 안 돌아가면,
//   결과가 있는데도 "조건에 맞는 설비가 없습니다" 가 나옵니다.
//
// 다 됐으면 기계로도 확인하세요.
//   node _검증도구/화면검증.js 10_프론트엔드_연동


// ============================================================
// 다 만든 뒤 답해 보세요
// ============================================================
//
// ① 3번에서 1쪽으로 안 돌리면 서버는 무엇을 돌려주나요? 화면에는요?
//    __________________________________________
//
// ② 삭제 버튼에 이벤트 위임을 쓴 이유는?
//    표를 다시 그린 뒤에도 버튼이 동작하나요?
//    __________________________________________
//
// ③ 삭제한 뒤 목록을 다시 부르는 것과, 화면에서만 그 줄을 지우는 것.
//    각각 무엇이 좋고 무엇이 나쁜가요?
//    __________________________________________
//
// ④ 9번에서 409 를 어디에 보여 줬나요? 왜 거기인가요?
//    __________________________________________
//
// ⑤ 검색창의 디바운스를 지우면 요청이 몇 배로 늘어나나요?
//    F12 → Network 에서 세어 보세요.
//    __________________________________________
//
// ⑥ 이 화면에 업로드를 붙인다면 무엇을 더 챙겨야 하나요? (개념05)
//    __________________________________________


// ============================================================
// 잘 안 될 때 보는 곳
// ============================================================
//
// 등록 버튼을 눌렀는데 페이지가 새로고침됨
//   → event.preventDefault() 를 안 했습니다.
//
// 검색했는데 "설비가 없습니다"
//   → 조건이 바뀌었는데 page 를 1 로 안 돌렸습니다.
//
// 고쳤는데 빨간 글씨가 그대로
//   → 보내기 전에 지난 오류를 안 지웠습니다.
//
// 표를 다시 그린 뒤 삭제 버튼이 안 먹음
//   → 버튼마다 addEventListener 를 달았습니다. tbody 에 위임하세요.
//
// 화면이 뜨자마자 "설비가 없습니다" 가 깜빡임
//   → 로딩 상태를 안 다뤘습니다. 판단 순서를 확인하세요.
//
// 빠르게 검색하면 결과가 검색어와 안 맞음
//   → 늦게 온 응답을 안 버렸습니다. (개념02 섹션 4)
//
// 요청이 글자마다 나감
//   → 디바운스를 안 걸었습니다.
