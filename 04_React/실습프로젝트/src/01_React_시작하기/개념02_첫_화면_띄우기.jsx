// ============================================================
// 01단원 · 개념 02 — 첫 화면 띄우기
// ------------------------------------------------------------
// 실행: 실습프로젝트에서 npm run dev → 왼쪽 목록에서 이 예제를 고르세요.
//       F12 → Console 도 함께 보세요.
// ============================================================
//
// 개념01에서는 React 방식이 어떻게 생겼는지 눈으로만 봤습니다.
// 이 파일부터는 "그 화면이 어떻게 거기 나타났는지" 를 따라갑니다.
//
// 지금 여러분 브라우저에 보이는 이 글자들은 저절로 생긴 것이 아닙니다.
// 파일 네 개를 거쳐서 왔습니다.
//
//   index.html  →  src/main.jsx  →  src/App.jsx  →  지금 이 파일
//
// 이 파일에서는 그 네 단계를 하나씩 열어 봅니다.
// ★ 앞의 세 개는 자료가 미리 만들어 둔 것입니다. 고칠 일이 없습니다.
//   그래도 한 번은 읽어 두세요. 화면이 안 나올 때 볼 곳이 거기입니다.

import Summary from "../_ui/Summary.jsx";
import 그려진뒤 from "../_ui/그려진뒤.js";

// ── 섹션 1: index.html — 시작하는 한 장 ──

// 프로젝트 폴더 맨 위에 index.html 이 한 장 있습니다. 열어 보세요.
// 길지 않습니다. 중요한 것은 두 줄뿐입니다.
//
//     <div id="root"></div>
//     <script type="module" src="/src/main.jsx"></script>
//
// 첫째 줄은 '빈 자리' 입니다. 안에 아무것도 없습니다.
// 둘째 줄은 "이 자바스크립트 파일부터 실행해라" 입니다.
//
// 여러분이 npm run dev 로 띄운 주소(http://localhost:5173 같은 것)를 열면
// 브라우저가 받는 것은 이 index.html 한 장입니다.
// 그 순간 화면에는 아무것도 없습니다. 빈 div 하나뿐이니까요.
//
// ★ 페이지 소스 보기(Ctrl+U)로 확인할 수 있습니다.
//   지금 화면에는 글자가 잔뜩 있는데, 소스에는 그 글자가 하나도 없습니다.
//   화면에 보이는 것은 전부 자바스크립트가 나중에 만들어 넣은 것입니다.

// ✏️ 직접 해보기 1 — Ctrl+U 로 페이지 소스를 열어 보세요.
//                    "개념 02" 라는 글자가 소스 안에 있는지 찾아보세요.
//                    (정답은 파일 맨 아래에)

// ── 섹션 2: React 가 그림을 그릴 자리 ──

// 그 빈 div 가 React 가 그림을 그릴 자리입니다.
//
//     <div id="root"></div>
//
// React 도 결국 브라우저 화면(DOM)에 그려야 하는데,
// "어디에" 그릴지는 React 가 알 수 없습니다. 우리가 알려 줘야 합니다.
// JS자료 10단원에서 목록을 만들 때도 항상 이렇게 시작했습니다.
//
//     const list = document.querySelector("#list");
//
// 같은 일입니다. 먼저 자리를 찾고, 그 자리에 그립니다.
//
// id 가 꼭 root 여야 할까요? 아닙니다. 아무 이름이나 됩니다.
// 다만 거의 모든 React 프로젝트가 root 를 쓰기 때문에 그대로 따랐습니다.
//
// 한 가지만 기억하세요.
// 그 안은 이제 React 가 통째로 맡습니다.
// 그래서 보통은 <div id="root"></div> 처럼 비워 둡니다.

// 코드로 확인해 봅시다. 그 자리는 지금도 페이지에 있습니다.

const 뿌리자리 = document.querySelector("#root");

console.log(뿌리자리.tagName);
// 콘솔: DIV

console.log(뿌리자리.id);
// 콘솔: root

// 안이 비어 있지 않습니다. React 가 이미 잔뜩 그려 넣었기 때문입니다.
console.log(뿌리자리.children.length > 0);
// 콘솔: true

// ✏️ 직접 해보기 2 — 위 세 줄 아래에 이 줄을 더해 보세요.
//                    console.log(뿌리자리.parentElement.tagName);
//                    무엇이 찍힐지 먼저 예상해 보세요.

// ── 섹션 3: createRoot 와 render — 프로젝트 전체에 딱 한 번 ──

// 자리를 찾았으니 이제 그리라고 말해야 합니다. 두 줄이면 됩니다.
//
//     const root = createRoot(자리);
//     root.render(화면설명);
//
// 두 줄이 각각 무슨 일을 하는지 나눠 봅시다.
//
// [createRoot(자리)]
//   "이 자리는 이제부터 내가 맡겠다" 고 React 에게 알려 주는 줄입니다.
//   한 자리에 대해 딱 한 번만 부릅니다. 두 번 부르면 경고가 납니다.
//   돌려주는 것은 root 라는 물건입니다. 이 안에 render 가 들어 있습니다.
//
// [root.render(화면설명)]
//   실제로 그려 달라고 부탁하는 줄입니다.
//   괄호 안에 "무엇이 보여야 하는지" 를 넣습니다.
//
// 이 두 줄은 이미 쓰여 있습니다. src/main.jsx 를 열어 보세요.
//
//     import { createRoot } from "react-dom/client";
//     import App from "./App.jsx";
//
//     createRoot(document.querySelector("#root")).render(
//       <StrictMode>
//         <App />
//       </StrictMode>
//     );
//
// ★ 이 두 줄은 프로젝트 전체에서 딱 한 번만 나옵니다.
//   화면이 아무리 많아져도 createRoot 는 늘 한 번입니다.
//   09단원까지 가도, 13단원 종합 프로젝트에서도 여기 이 한 줄뿐입니다.
//
// 그래서 여러분이 만드는 파일에는 createRoot 가 나오지 않습니다.
// 여러분은 "무엇이 보여야 하는지" 만 적고, 그리는 일은 main.jsx 가 맡습니다.

// ✏️ 직접 해보기 3 — src/main.jsx 를 열어 createRoot 가 몇 번 나오는지 세어 보세요.
//                    그리고 이 프로젝트 전체(src 폴더)에서 몇 번 나오는지도요.

// ── 섹션 4: render 는 부탁이지 명령이 아닙니다 ──

// 여기서 꼭 짚고 갈 것이 하나 있습니다.
//
// render 는 "이렇게 그려 주세요" 하고 부탁만 하고 바로 다음 줄로 넘어갑니다.
// 실제로 화면이 만들어지는 것은 아주 잠깐 뒤입니다.
//
// 그래서 render 를 부른 바로 다음 줄에서 그 자리를 읽으면 아직 비어 있습니다.
// 이것 때문에 처음에 많이 헤맵니다. 눈으로 확인해 봅시다.

const 새자리 = document.createElement("div");
새자리.id = "잠깐시험";
document.body.appendChild(새자리);

console.log("만든 직후 안쪽:", JSON.stringify(새자리.innerHTML));
// 콘솔: 만든 직후 안쪽: ""

// 이 자료에는 그 기다림을 대신해 주는 도우미가 있습니다.
// src/_ui/그려진뒤.js 입니다. 읽지 않아도 됩니다.
// "그 자리가 생기면 한 번 실행해 줘" 라는 뜻입니다.

그려진뒤("#정리자리", (자리) => {
  console.log("React 가 그린 뒤 안쪽 글자 수:", 자리.innerText.length > 0);
  // 콘솔: React 가 그린 뒤 안쪽 글자 수: true
  새자리.remove();
});

// 정리하면 이렇습니다.
//
//   화면이 바뀌었는지는 콘솔로 읽지 말고 눈으로 보세요.
//
// 이 자료에서 화면 결과를 // 화면: 으로 따로 적는 이유가 이것입니다.
// 콘솔로 확인하려 들면 "아직 안 그려진" 상태를 보게 됩니다.

// ✏️ 직접 해보기 4 — 위 그려진뒤 안의 "#정리자리" 를 "#없는자리" 로 바꿔 보세요.
//                    콘솔에 무엇이 찍힐까요? 에러가 날까요?

// ── 섹션 5: 화면 설명을 함수로 묶기 ──

// 화면 설명이 길어지면 함수 하나로 묶습니다.
// 그 함수를 컴포넌트라고 부릅니다. 03단원에서 제대로 배웁니다.

function 인사() {
  return <h3>안녕하세요, React 입니다</h3>;
}

// 규칙이 두 개 있습니다. 지금 외우지 않아도 됩니다. 04단원까지 계속 나옵니다.
//
//   (1) 이름은 대문자나 한글로 시작합니다. 소문자로 시작하면 조용히 안 나옵니다.
//   (2) 쓸 때는 <인사 /> 처럼 꺾쇠를 씌웁니다. 인사() 라고 부르지 않습니다.
//
// 이 파일이 화면에 내놓는 것은 맨 아래 export default 한 덩어리입니다.
// 왼쪽 목록에서 이 예제를 고르면 App.jsx 가 그것을 가져다 화면에 넣습니다.
//
//     export default function Concept02() { ... }
//                     ↑ "이 파일이 내놓는 것은 이겁니다" 라는 뜻입니다
//
// export 와 import 는 08단원에서 제대로 배웁니다.
// 지금은 "파일마다 하나씩 내놓는다" 정도로 보세요.

console.log(typeof 인사);
// 콘솔: function

// ✏️ 직접 해보기 5 — 인사 함수의 <h3> 를 <h1> 으로 바꾸고 저장해 보세요.
//                    저장하는 순간 아래 ② 상자의 글자가 커집니다.

// ── 섹션 6: 저장하면 바로 바뀝니다 ──

// 고친 뒤에 새로고침(F5)을 누를 필요가 없습니다.
// npm run dev 가 파일을 지켜보고 있다가, 바뀐 곳만 화면에 갈아 끼웁니다.
//
// ★ 오히려 F5 는 누르지 마세요.
//   왼쪽 목록에서 고른 예제가 풀려서 맨 처음 예제로 돌아갑니다.
//
// 화면이 안 바뀐다면 순서대로 보세요.
//
//   (1) 저장을 했나 (Ctrl+S)
//   (2) 터미널에 빨간 글자가 떴나  ← 문법이 틀리면 여기 먼저 뜹니다
//   (3) F12 → Console 에 빨간 줄이 있나
//   (4) npm run dev 가 아직 돌고 있나  ← 터미널을 닫았으면 꺼진 것입니다
//
// (2)를 잊지 마세요. 처음 배울 때 가장 많이 놓치는 곳입니다.
// 브라우저만 보고 있으면 "아무 일도 안 일어난다" 로 보이는데,
// 터미널에는 이미 무엇이 틀렸는지 적혀 있습니다.

// ✏️ 직접 해보기 6 — npm run dev 를 켜 둔 터미널에서 Ctrl+C 로 서버를 끄고,
//                    브라우저에서 F5 를 눌러 보세요. 무엇이 보입니까?
//                    확인했으면 다시 npm run dev 로 켜세요.

// ── 섹션 7: 자주 하는 실수 ──

// [실수 1] export default 를 안 붙인다
//
//   function Concept02() { ... }        ← default 가 없습니다
//
// 실수: 왼쪽 목록에서 골라도 화면이 비고 빨간 상자가 뜹니다.
//       App.jsx 는 "이 파일이 내놓는 것" 을 찾는데 내놓은 게 없기 때문입니다.

// [실수 2] 한 파일에서 export default 를 두 번 쓴다
//
// 실수: 터미널이 멈춥니다. Duplicate export of 'default'
//       내놓는 것은 파일마다 하나입니다.

// [실수 3] createRoot 를 내 파일에도 쓴다
//
//   const root = createRoot(document.querySelector("#root"));   ← 하지 마세요
//
// 실수: 이런 경고가 납니다.
//         You are calling ReactDOMClient.createRoot() on a container
//         that has already been passed to createRoot() before.
//       그 자리는 이미 main.jsx 가 맡았습니다. 두 번 맡을 수 없습니다.
//       여러분 파일은 export default 만 내놓으면 됩니다.

// [실수 4] 컴포넌트 이름을 소문자로 시작한다
//
//   function 인사() {}   → <인사 />      한글은 됩니다
//   function greet() {}  → <greet />     소문자 영문은 안 됩니다
//
// 실수: 에러도 경고도 없이 화면만 빕니다. 개념03에서 정면으로 다룹니다.

// 검증: 선행문법허용 시작 — 화면 모양을 만드는 부분입니다 (className 은 02단원, <> 는 02단원, children 은 03단원에서 배웁니다)
export default function Concept02() {
  return (
    <div>
      <h1>개념 02 — 첫 화면 띄우기</h1>

      <p className="guide">
        왼쪽 목록에서 이 예제를 고르면 이 화면이 나옵니다.{" "}
        <strong>F12 → Console</strong> 도 함께 보세요.
        <br />
        <br />
        <strong>이 파일은 직접 고쳐 보는 파일입니다.</strong> 고치고{" "}
        <strong>저장(Ctrl+S)</strong> 하면 화면이 바로 바뀝니다. 새로고침(F5)은
        누르지 마세요 — 왼쪽에서 고른 예제가 풀립니다.
        <br />
        <br />
        <strong>이 파일이 말하는 파일 세 개를 같이 열어 두세요.</strong>{" "}
        <code>index.html</code> · <code>src/main.jsx</code> ·{" "}
        <code>src/App.jsx</code> 입니다. 셋 다 고칠 일은 없습니다.
      </p>

      <div className="demo">
        <h3>① 화면이 여기까지 오는 길</h3>
        <div className="output">
          <p>
            <code>index.html</code> 의 <code>&lt;div id="root"&gt;</code>
          </p>
          <p>↓ 그 자리를 맡는다</p>
          <p>
            <code>src/main.jsx</code> 의 <code>createRoot(...).render(...)</code>
          </p>
          <p>↓ 무엇을 그릴지 넘긴다</p>
          <p>
            <code>src/App.jsx</code> — 왼쪽 목록을 그리고, 고른 예제를 옆에 놓는다
          </p>
          <p>↓ 고른 예제의 export default 를 가져다 놓는다</p>
          <p>
            <strong>지금 읽고 있는 이 파일</strong>
          </p>
        </div>
      </div>

      <div className="demo">
        <h3>② 화면 설명을 함수로 묶은 것</h3>
        <div className="output">
          <인사 />
        </div>
      </div>

      <div id="정리자리">
        <Summary
          items={[
            <>
              <code>index.html</code> 에는 빈 <code>&lt;div id="root"&gt;</code> 하나만
              있습니다. 화면에 보이는 글자는 전부 자바스크립트가 나중에 만들어 넣은
              것입니다.
            </>,
            <>
              <code>createRoot(자리)</code> 는 자리를 맡는 줄,{" "}
              <code>root.render(화면설명)</code> 은 그려 달라는 줄입니다.
            </>,
            <>
              그 두 줄은 <strong>프로젝트 전체에 딱 한 번</strong>,{" "}
              <code>src/main.jsx</code> 에만 있습니다. 여러분 파일에는 쓰지 않습니다.
            </>,
            <>
              여러분 파일이 할 일은 <code>export default</code> 로 화면 설명 하나를
              내놓는 것입니다.
            </>,
            "render 는 부탁입니다. 부른 바로 다음 줄에서 그 자리를 읽으면 아직 비어 있습니다. 화면 결과는 눈으로 확인하세요.",
            "고치고 저장하면 화면이 바로 바뀝니다. 안 바뀌면 ① 저장했나 ② 터미널에 빨간 글자가 있나 ③ 콘솔에 빨간 줄이 있나 순서로 보세요.",
          ]}
        />
      </div>
    </div>
  );
}
// 검증: 선행문법허용 끝

// ============================================================
// 직접 해보기 정답
// ============================================================
//
// 1) 없습니다. 소스에는 <div id="root"></div> 만 있습니다.
//    "개념 02" 라는 글자는 이 파일의 <h1> 이 만든 것이고,
//    그것은 브라우저가 자바스크립트를 실행한 뒤에 생깁니다.
//    → 그래서 페이지 소스와 화면이 다릅니다. 개발자 도구의 Elements 탭을 보면
//      지금 화면 그대로가 보입니다. 그쪽은 '실행된 뒤' 를 보여 주기 때문입니다.
//
// 2) BODY 가 찍힙니다.
//    console.log(뿌리자리.parentElement.tagName);
//    // 콘솔: BODY
//    → index.html 에서 그 div 는 body 바로 아래에 있습니다.
//
// 3) main.jsx 에 1번, src 폴더 전체에도 1번입니다.
//    터미널에서 이렇게 세어 볼 수도 있습니다.
//      findstr /s /c:"createRoot" src\*.jsx      (윈도우)
//    → 예제 파일 안에 나오는 것은 전부 주석 속 설명입니다. 실제 호출은 한 번뿐입니다.
//
// 4) 아무것도 안 찍히고, 에러도 안 납니다.
//    그려진뒤 는 그 자리를 못 찾으면 3초쯤 기다려 보다가 조용히 그만둡니다.
//    → "조용히 아무 일도 안 일어난다" 가 가장 찾기 어려운 고장입니다.
//      그래서 이 자료는 화면 결과를 // 화면: 으로 따로 적어 둡니다.
//
// 6) 브라우저에 "사이트에 연결할 수 없음" 이 나옵니다.
//    → npm run dev 는 명령 한 번으로 끝나는 것이 아니라 서버를 켜 두는 것입니다.
//      그 터미널이 서버입니다. 닫으면 화면도 안 뜹니다.
//      "어제까지 되던 게 오늘 안 된다" 의 거의 전부가 이것입니다.
//
// 5) ② 상자의 "안녕하세요, React 입니다" 가 큰 글씨가 됩니다.
//    저장하는 순간 바뀝니다. F5 는 누르지 않아도 됩니다.
//    → 안 바뀌었다면 저장이 안 됐거나(제목 옆에 점이 있는지 보세요),
//      터미널에 빨간 글자가 떠 있습니다.
