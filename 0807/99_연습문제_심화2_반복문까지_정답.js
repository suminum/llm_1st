// 99_연습문제 심화2 정답 (JS 기초 ~ 반복문까지)
// node 99_연습문제_심화2_반복문까지_정답.js 로 실행하며 결과를 확인합니다.
// 각 문제를 중괄호 블록 { } 으로 감싸 변수 이름 충돌을 막았습니다.
// 정답 코드보다 아래의 "해설:"을 읽는 것이 중요합니다.

// ═══ 워밍업 A 정답 ═══ for 로 배열 순회하며 누적
console.log("===== 워밍업 A =====");
{
  const steps = [5000, 7200, 4300, 8100, 6400, 3000, 8000];

  let total = 0; // ★ 반복문 밖에 선언
  for (let i = 0; i < steps.length; i++) {
    total += steps[i];
  }

  console.log(`합계: ${total}걸음`); // 출력: 합계: 42000걸음
  console.log(`평균: ${total / steps.length}걸음`); // 출력: 평균: 6000걸음

  // 해설 ①: total 을 반복문 안에 선언하면 매 회차 0으로 초기화되어 누적이 안 됩니다.
  //   "밖에서 0으로 시작 → 안에서 += → 끝난 뒤 사용"이 누적 3단 세트입니다.
  //
  // 해설 ②: 조건에 7이라는 숫자를 직접 쓰지 않고 steps.length 를 쓴 이유 —
  //   요일이 하나 늘어도 반복문을 고칠 필요가 없습니다.
  //
  // 해설 ③: 평균은 반복이 끝난 뒤에 한 번만 계산합니다.
  //   반복문 안에서 나누면 회차마다 엉뚱한 값이 나옵니다.
}

// ═══ 워밍업 B 정답 ═══ for 안에 if 넣기
console.log("===== 워밍업 B =====");
{
  const steps2 = [5000, 7200, 4300, 8100, 6400, 3000, 8000];

  let achieveCount = 0;
  for (let i = 0; i < steps2.length; i++) {
    if (steps2[i] >= 6000) {
      console.log(`${i + 1}일차: ${steps2[i]}걸음`); // ★ i + 1 로 사람 기준 번호
      achieveCount++;
    }
  }
  console.log(`목표 달성: ${achieveCount}일`); // 출력: 목표 달성: 4일
  // 출력: 2일차 / 4일차 / 5일차 / 7일차

  // 해설 ①: 반복문 + 조건문 = 오늘 가장 많이 쓰는 조합입니다.
  //   "전부 돌면서(for) 조건에 맞는 것만 처리한다(if)".
  //
  // 해설 ②: i 는 0부터라 첫날이 0일차가 되어 버립니다.
  //   화면에 보이는 번호는 사람 기준(1부터)이라 i + 1 로 맞춥니다.
  //   반대로 배열에서 값을 꺼낼 때는 반드시 i 를 그대로 써야 합니다 — steps2[i + 1] 이 아닙니다.
  //
  // 해설 ③: achieveCount++ 를 if 안에 둔 것이 중요합니다.
  //   밖에 두면 조건과 상관없이 7이 됩니다.
}

// ═══ 워밍업 C 정답 ═══ while 로 반복
console.log("===== 워밍업 C =====");
{
  let count = 5;
  while (count >= 1) {
    console.log(count);
    count--; // ★ 이 줄이 없으면 무한 루프
  }
  console.log("발사!");
  // 출력: 5 / 4 / 3 / 2 / 1 / 발사!

  // 해설 ①: 세 곳이 모두 "줄어드는 쪽"으로 맞아야 합니다.
  //   시작값 5 (큰 수) / 조건 >= 1 (부등호 방향) / count-- (감소)
  //   하나만 어긋나도 한 번도 안 돌거나 영원히 돕니다.
  //   예: 조건을 count <= 1 로 쓰면 처음부터 거짓이라 아무것도 출력되지 않습니다.
  //
  // 해설 ②: count-- 를 빠뜨리면 5가 영원히 찍힙니다.
  //   for 는 증감이 소괄호 안에 있어 빠뜨리기 어렵지만, while 은 직접 챙겨야 합니다.
  //   터미널이 멈추지 않으면 Ctrl + C 로 강제 종료하세요.
  //
  // 해설 ③: "발사!"는 반복문 밖에 있습니다.
  //   안에 두면 숫자마다 한 번씩, 총 다섯 번 찍힙니다.
}

// ═══ 워밍업 D 정답 ═══ for 의 세 칸을 바꿔 쓰기
console.log("===== 워밍업 D =====");
{
  // ① 역순 — 세 칸이 모두 "줄어드는 쪽"
  for (let rank = 5; rank >= 1; rank--) {
    //       └큰 수부터  └부등호도 뒤집기  └감소
    console.log(`${rank}위`);
  }
  // 출력: 5위 / 4위 / 3위 / 2위 / 1위

  // ② 2씩 건너뛰기 — 증감만 바꾸면 됨
  for (let no = 1; no <= 10; no += 2) {
    console.log(`대기 ${no}번`);
  }
  // 출력: 대기 1번 / 3번 / 5번 / 7번 / 9번

  // 해설 ①: 역순에서 흔한 실수는 부등호를 그대로 두는 것입니다.
  //   for (let rank = 5; rank <= 1; rank--) → 처음부터 5 <= 1 이 거짓이라 한 번도 안 돕니다.
  //   에러가 아니라 "아무것도 출력되지 않는" 형태라 원인을 찾기 어렵습니다.
  //   시작값·조건·증감 세 곳이 같은 방향인지 항상 확인하세요.
  //
  // 해설 ②: 증감은 1씩일 필요가 없습니다. i += 2 면 두 칸씩 건너뜁니다.
  //   1부터 시작해 2씩 더하면 홀수만, 2부터 시작하면 짝수만 나옵니다.
  //   (같은 일을 i++ 로 다 돌면서 if (no % 2 === 1) 로 걸러도 됩니다.
  //    결과는 같지만 이쪽이 반복 횟수가 절반입니다)
  //
  // 해설 ③: 10까지인데 9에서 끝나는 이유 —
  //   9 다음은 11이 되어 조건 11 <= 10 이 거짓이 됩니다. 10은 건너뛴 것이 아니라 안 밟은 것입니다.
}

// ═══ 문제 1 정답 ═══ 배열 통계 한 번에 구하기
console.log("===== 문제 1 =====");
{
  const scores = [78, 92, 45, 88, 61, 95, 73];

  let sum = 0;
  let max = scores[0]; // ★ 시작값을 첫 요소로
  let min = scores[0];
  let highCount = 0;

  for (let i = 0; i < scores.length; i++) {
    sum += scores[i];
    if (scores[i] > max) max = scores[i];
    if (scores[i] < min) min = scores[i];
    if (scores[i] >= 80) highCount++;
  }

  console.log(`평균 점수: ${sum / scores.length}점`); // 출력: 평균 점수: 76점
  console.log(`최고 점수: ${max}점`); // 출력: 최고 점수: 95점
  console.log(`최저 점수: ${min}점`); // 출력: 최저 점수: 45점
  console.log(`80점 이상: ${highCount}명`); // 출력: 80점 이상: 3명

  // 해설 ①: 시작값이 이 문제의 핵심입니다.
  //   min을 0으로 두면 어떤 점수도 0보다 작지 않아 갱신이 한 번도 일어나지 않고 0이 남습니다.
  //   max를 0으로 두는 건 이 경우엔 우연히 되지만, 음수가 섞인 배열에서는 똑같이 깨집니다.
  //   그래서 "배열의 첫 요소"로 시작하는 것이 안전합니다 — 어떤 값이 와도 통합니다.
  //
  // 해설 ②: 집계 변수(sum, max, min, highCount)는 전부 반복문 "밖"에 선언했습니다.
  //   안에 선언하면 매 회차 초기화되어 누적이 안 됩니다.
  //
  // 해설 ③: for 하나로 네 가지를 동시에 집계했습니다.
  //   배열을 네 번 돌 필요가 없습니다 — 한 바퀴 도는 동안 할 일을 다 합니다.
}

// ═══ 문제 2 정답 ═══ break 로 찾기 + 못 찾았을 때
console.log("===== 문제 2 =====");
{
  const stocks = [12, 8, 3, 5, 0];

  let foundIndex = -1; // ★ "아직 못 찾음"을 -1로 표시

  for (let i = 0; i < stocks.length; i++) {
    if (stocks[i] < 5) {
      foundIndex = i;
      break; // 찾았으니 나머지는 볼 필요 없음
    }
  }

  if (foundIndex === -1) {
    console.log("전 품목 재고 충분");
  } else {
    console.log(`${foundIndex}번 상품 재고 부족`); // 출력: 2번 상품 재고 부족
  }

  // 해설 ①: 왜 -1로 시작하는가 —
  //   인덱스는 0부터라서 0이 "못 찾음"을 뜻할 수 없습니다. 0은 "맨 앞에서 찾았다"는 뜻이니까요.
  //   그래서 인덱스가 될 수 없는 음수를 "없음"의 표시로 씁니다.
  //   10일차에 배울 indexOf 도 못 찾으면 -1을 주는데, 정확히 같은 이유입니다.
  //
  // 해설 ②: foundIndex를 반복문 밖에 선언한 이유 —
  //   반복이 끝난 뒤에도 결과를 써야 하기 때문입니다.
  //   for 소괄호나 블록 안에서 선언하면 반복이 끝나는 순간 사라집니다.
  //
  // 해설 ③: break가 없어도 답은 같지만, 배열이 10만 개라면 찾은 뒤에도 끝까지 돕니다.
  //   "찾으면 즉시 끝낸다"가 break의 존재 이유입니다.
  //   (단, break를 쓰면 "마지막" 항목이 아니라 "처음" 항목을 찾게 된다는 점에 주의)
}

// ═══ 문제 3 정답 ═══ 중첩 for + continue + break
console.log("===== 문제 3 =====");
{
  for (let dan = 2; dan <= 5; dan++) {
    if (dan % 2 === 1) continue; // 홀수 단은 통째로 건너뜀 (3단, 5단)

    for (let n = 1; n <= 9; n++) {
      if (dan * n > 20) break; // 20을 넘으면 이 단은 여기서 중단
      console.log(`${dan} x ${n} = ${dan * n}`);
    }
  }
  // 출력: 2단 9줄 (2~18, 전부 20 이하) + 4단 5줄 (4~20, 4x6=24에서 중단)

  // 해설 ①: continue는 "바깥" 반복문 안에 있으므로 그 단 전체를 건너뜁니다.
  //   dan이 3일 때 안쪽 반복문에 들어가지도 않습니다.
  //
  // 해설 ②: break는 "안쪽" 반복문 안에 있으므로 그 단의 곱셈만 멈춥니다.
  //   4x6에서 break가 걸려도 바깥 dan은 계속 돌아 5단으로 넘어갑니다.
  //   ★ 중첩에서 break는 가장 안쪽 반복문 하나만 끝냅니다. 바깥까지 멈추지 않습니다.
  //
  // 해설 ③: 두 키워드의 위치가 결과를 결정합니다.
  //   continue를 안쪽에 뒀다면 "그 곱셈 한 줄만" 건너뛰었을 것이고,
  //   break를 바깥에 뒀다면 그 단에서 구구단 전체가 끝났을 것입니다.
}

// ═══ 문제 4 정답 ═══ while 로 "언제 끝날지 모르는" 반복
console.log("===== 문제 4 =====");
{
  // (1) 매일 3500원만 쓰는 경우
  let balance = 50000;
  let day = 0;
  while (balance >= 3500) {
    balance -= 3500;
    day++;
  }
  console.log(`버틴 일수: ${day}일`); // 출력: 버틴 일수: 14일
  console.log(`남은 잔액: ${balance}원`); // 출력: 남은 잔액: 1000원

  // (2) 5일마다 커피값 4000원 추가
  let balance2 = 50000;
  let day2 = 0;
  while (balance2 >= 3500) {
    balance2 -= 3500;
    day2++;
    if (day2 % 5 === 0) balance2 -= 4000; // 5·10일째에 추가 지출
  }
  console.log(`(커피 포함) 버틴 일수: ${day2}일`); // 출력: (커피 포함) 버틴 일수: 12일

  // 해설 ①: 왜 while인가 —
  //   for는 "몇 번 돌지" 미리 알 때 씁니다. 여기서는 며칠 버틸지가 계산 결과라서 모릅니다.
  //   "잔액이 남아 있는 동안"이라는 조건만 알 뿐입니다. 이럴 때 while을 씁니다.
  //
  // 해설 ②: 조건은 매 회차 "시작 전"에 검사합니다.
  //   14일째에 잔액이 1000원이 되고, 15일째로 들어가기 전 1000 >= 3500 이 거짓이라 멈춥니다.
  //   그래서 잔액이 음수가 되지 않습니다.
  //
  // 해설 ③: day++ 를 빠뜨리면 무한 루프가 아니라 "일수만 0으로 남는" 버그가 됩니다.
  //   (잔액은 계속 줄어드니 반복은 언젠가 끝납니다) — 무한 루프보다 찾기 어려운 종류입니다.
  //
  // 해설 ④: (2)에서 커피값을 뺀 뒤 조건을 다시 검사하기까지 한 회차가 필요합니다.
  //   그래서 잔액이 3500 밑으로 떨어져도 그 회차는 이미 지나간 뒤입니다.
}

// ═══ 문제 5 정답 ═══ [도전] 중첩 for 로 모양 만들기
console.log("===== 문제 5 =====");
{
  for (let line = 1; line <= 5; line++) {
    let stars = ""; // ★ 줄이 바뀔 때마다 빈 문자열에서 다시 시작
    for (let s = 1; s <= line; s++) {
      stars += "*"; // 문자열도 += 로 이어 붙입니다
    }
    console.log(stars);
  }
  // 출력: * / ** / *** / **** / *****

  // 해설 ①: 안쪽 반복 횟수가 바깥 변수 line에 달려 있습니다 (s <= line).
  //   지금까지 중첩 for는 안쪽이 늘 1~9로 고정이었는데, 여기서는 줄마다 달라집니다.
  //   이것이 모양 만들기 문제의 핵심입니다.
  //
  // 해설 ②: stars 를 안쪽 반복문 "밖", 바깥 반복문 "안"에 선언한 위치가 중요합니다.
  //   더 밖(바깥 반복문 밖)에 두면 이전 줄의 별이 계속 쌓여 *, ***, ****** 가 됩니다.
  //   더 안(안쪽 반복문 안)에 두면 매번 초기화되어 * 만 다섯 줄 나옵니다.
  //   "줄이 바뀔 때 초기화"가 필요하므로 지금 위치가 맞습니다.
  //
  // 해설 ③: console.log 는 부를 때마다 줄을 바꿉니다.
  //   그래서 한 줄을 완성한 뒤에 한 번만 불러야 합니다.
  //   안쪽에서 부르면 별이 세로로 한 개씩 찍힙니다.
}

// ═══ 문제 6 정답 ═══ [도전] 결과 예측하기
console.log("===== 문제 6 =====");
{
  // (가) 안에서 같은 이름을 다시 선언하면
  let point = 0;
  for (let i = 1; i <= 3; i++) {
    let point = 0; // ★ 바깥 point와 이름만 같은 "다른 변수"
    point += i;
  }
  console.log(point); // 출력: 0
  // 해설: 안쪽 let point 는 매 회차 새로 만들어지고 회차가 끝나면 사라집니다.
  //   바깥 point 는 한 번도 건드려지지 않아 0 그대로입니다.
  //   누적이 안 될 때 "안에서 다시 선언하지 않았는가"를 확인하세요. 9일차 최다 실수입니다.

  // (나) continue와 break가 같이 있을 때
  for (let i = 0; i < 5; i++) {
    if (i === 2) continue; // 2만 건너뛰고 계속
    if (i === 4) break; // 4에서 완전히 종료
    console.log(i);
  }
  // 출력: 0
  // 출력: 1
  // 출력: 3
  // 해설: i=2는 continue라 출력을 건너뛰지만 반복은 계속됩니다.
  //   i=4는 break라 반복 자체가 끝나 4는 출력되지 않습니다.
  //   "건너뛰기"와 "나가기"의 차이가 한 화면에 보입니다.

  // (다) 중첩에서 break는 어디를 끝내는가
  for (let i = 0; i < 2; i++) {
    for (let j = 0; j < 3; j++) {
      if (j === 1) break; // 안쪽만 끝냄
      console.log(i, j);
    }
  }
  // 출력: 0 0
  // 출력: 1 0
  // 해설: j가 1이 되는 순간 안쪽 반복이 끝나지만, 바깥 i는 계속 돌아 다음 회차로 갑니다.
  //   그래서 j는 늘 0에서만 출력되고, i는 0과 1 둘 다 나옵니다.

  // (라) 반복문 안에서 선언한 i를 밖에서 쓰기 — 유일하게 에러가 나는 덩어리
  // for (let i = 0; i < 3; i++) { }
  // console.log(i);   → ReferenceError: i is not defined
  // ★ 이 정답 파일에서는 주석으로 두었습니다. 주석을 풀면 여기서 스크립트가 멈춰
  //   아래 문제 7 이 실행되지 않기 때문입니다. 직접 확인한 뒤에는 다시 주석 처리하세요.
  // 해설: let 으로 선언한 변수는 그 중괄호(또는 for 소괄호) 안에서만 유효합니다.
  //   반복이 끝나면 i는 사라집니다. 밖에서도 쓰려면 for 앞에 let i; 를 따로 선언해야 합니다.
  //   "에러가 난다"도 예측의 정답입니다 — 값이 아니라 에러가 나오는 경우를 알아보는 것이 목적입니다.
}

// ═══ 문제 7 정답 ═══ [도전] 종합 — 주문 목록 정산
console.log("===== 문제 7 =====");
{
  const orders = [
    { customer: "김철수", amount: 42000, status: "done" },
    { customer: "이영희", amount: 15000, status: "cancel" },
    { customer: "박민수", amount: 38000, status: "done" },
    { customer: "최지우", amount: 27000, status: "shipping" },
    { customer: "정수진", amount: 17000, status: "done" },
  ];

  let doneSum = 0;
  let cancelCount = 0;
  let maxAmount = orders[0].amount;
  let maxCustomer = orders[0].customer;

  // 1차: 집계
  for (let i = 0; i < orders.length; i++) {
    if (orders[i].status === "done") doneSum += orders[i].amount;
    if (orders[i].status === "cancel") cancelCount++;
    if (orders[i].amount > maxAmount) {
      maxAmount = orders[i].amount;
      maxCustomer = orders[i].customer; // ★ 금액과 이름을 함께 갱신
    }
  }

  console.log(`총 주문: ${orders.length}건`); // 출력: 총 주문: 5건
  console.log(`완료 금액 합계: ${doneSum}원`); // 출력: 완료 금액 합계: 97000원
  console.log(`취소: ${cancelCount}건`); // 출력: 취소: 1건
  console.log(`최고 금액 고객: ${maxCustomer}`); // 출력: 최고 금액 고객: 김철수

  // 2차: 완료 건만 등급 출력
  for (let i = 0; i < orders.length; i++) {
    if (orders[i].status !== "done") continue; // 완료가 아니면 건너뜀
    console.log(`${orders[i].customer}: ${orders[i].amount >= 30000 ? "VIP" : "일반"}`);
  }
  // 출력: 김철수: VIP
  // 출력: 박민수: VIP
  // 출력: 정수진: 일반

  // 해설 ①: 최댓값을 찾을 때 금액만 갱신하면 이름을 잃어버립니다.
  //   maxAmount 와 maxCustomer 를 "같은 if 안에서 함께" 바꿔야 짝이 맞습니다.
  //   이것이 문제 1의 최댓값 찾기보다 한 단계 어려운 지점입니다.
  //
  // 해설 ②: 반복문을 두 번 돈 이유 —
  //   집계(합계·개수·최댓값)는 전체를 다 봐야 결과가 나오고,
  //   등급 출력은 각 건마다 바로 판단할 수 있습니다. 성격이 달라 나눴습니다.
  //   한 번에 합칠 수도 있지만, 그러면 "최고 금액 고객"이 마지막에 출력되어 순서가 어긋납니다.
  //
  // 해설 ③: continue 로 "아닌 것을 먼저 쳐내는" 방식이
  //   if (status === "done") { ... } 로 감싸는 것보다 들여쓰기가 얕아 읽기 쉽습니다.
  //   9일차 섹션 6의 "중첩 펼치기"와 같은 발상입니다.
}
