// 99_연습문제 심화1 (JS 기초 ~ 조건문까지) — 08일차 + 09일차 조건문 파트 종합
// node 99_연습문제_심화1_조건문까지.js 로 실행하며 확인합니다.
// - 기존 99_연습문제보다 어렵습니다. 여러 개념을 한 문제에서 엮습니다.
// - 각 문제의 "// TODO: 여기에 작성" 부분을 채우면 (2) 기대 출력과 같은 결과가 나옵니다.
// - 사용 범위: 변수, 자료형, 연산자, 형 변환, 배열/객체 접근, if·else if·switch·삼항
//   (반복문 for/while, 함수는 아직 배우지 않았으므로 사용하지 않습니다!)

// ┌──────────────────────────────────────────────────────────┐
// │ 워밍업 A~C — 99_연습문제를 풀었다면 여기서 시작하세요.      │
// │ 문제 1부터가 본 문제입니다. 빠른 학생은 워밍업을 건너뛰어도 │
// │ 됩니다.                                                    │
// └──────────────────────────────────────────────────────────┘

// ═══ 워밍업 A ═══ 조건을 변수에 담아 쓰기 [기본]
// (1) 요구사항: 놀이기구 탑승 조건을 검사합니다.
//     - 키가 140 이상인지 판정해 isTallEnough 변수에 담고 출력하세요.
//     - 그 변수를 조건으로 if / else 를 써서 "탑승 가능" 또는 "탑승 불가"를 출력하세요.
//     ⚠️ if (height >= 140) 이라고 바로 쓰지 말고, 반드시 변수에 담아서 쓰세요.
//        조건에 이름을 붙이면 코드가 문장처럼 읽힙니다.
// (2) 기대 출력:
//     true
//     탑승 가능
// (3) 힌트: 개념코드 섹션06(조건에 이름 붙이기 - is~ 로 시작하는 변수명), 섹션02(if / else)
console.log("===== 워밍업 A =====");
const height = 152;
// TODO: 여기에 작성

// ═══ 워밍업 B ═══ 두 조건을 && 로 묶기 [기본]
// (1) 요구사항: 도서관 좌석 배정 조건입니다.
//     - "회원이면서 연체 도서가 없는" 경우에만 "좌석 배정 완료"를 출력하세요.
//     - 아니면 "좌석 배정 불가"를 출력하세요.
// (2) 기대 출력:
//     좌석 배정 불가
// (3) 힌트: 섹션03(&& - 둘 다 참이어야 참)
console.log("===== 워밍업 B =====");
const isLibraryMember = true;
const overdueCount = 2;
// TODO: 여기에 작성

// ═══ 워밍업 C ═══ else if 로 구간 나누기 [기본]
// (1) 요구사항: 미세먼지 수치로 등급 한 줄을 출력하세요.
//     - 151 이상: "매우 나쁨" / 81 이상: "나쁨" / 31 이상: "보통" / 그 외: "좋음"
//     - 반드시 하나만 출력되어야 합니다.
//     ⚠️ 어떤 조건을 맨 위에 둘지 생각하고 쓰세요.
// (2) 기대 출력:
//     나쁨
// (3) 힌트: 섹션04(else if - 좁은 범위=큰 값부터 위에)
console.log("===== 워밍업 C =====");
const dustLevel = 94;
// TODO: 여기에 작성

// ┌──────────────────────────────────────────────────────────┐
// │ 여기부터 본 문제입니다.                                    │
// └──────────────────────────────────────────────────────────┘

// ═══ 문제 1 ═══ 조건 세 개를 묶기 — && 와 || 섞어 쓰기 [응용]
// (1) 요구사항: 온라인 쇼핑몰 무료배송 판정입니다.
//     "회원이면서, (주문금액이 30000원 이상이거나 쿠폰을 가지고 있으면)" 무료배송입니다.
//     - 판정 결과를 isFreeShipping 변수에 담고 출력하세요.
//     - 그다음 그 결과에 따라 "무료배송" 또는 "배송비 3000원"을 출력하세요.
//     ⚠️ && 와 || 를 함께 쓸 때 괄호를 어디에 넣느냐로 결과가 달라집니다.
//        괄호 없이 써 보고, 있을 때와 결과를 비교해 보세요.
// (2) 기대 출력:
//     false
//     배송비 3000원
// (3) 힌트: 섹션03(논리 연산자), 8일차 섹션08(비교 결과를 boolean 변수에 담기)
console.log("===== 문제 1 =====");
const isMember = false;
const orderAmount = 52000;
const hasCoupon = true;
// TODO: 여기에 작성

// ═══ 문제 2 ═══ else if 순서가 틀린 코드 고치기 [응용]
// (1) 요구사항: 아래 배송 등급 판정은 "항상 일반 배송"만 출력됩니다.
//     주석 안의 코드를 읽고 왜 그런지 파악한 뒤, 아래에 고친 코드를 작성하세요.
//
//       if (deliveryCount >= 1)       { console.log("일반 배송"); }
//       else if (deliveryCount >= 10) { console.log("우수 고객 무료배송"); }
//       else if (deliveryCount >= 50) { console.log("VIP 당일배송"); }
//
//     - deliveryCount 가 63 일 때 "VIP 당일배송"이 나오도록 고치세요.
// (2) 기대 출력:
//     VIP 당일배송
// (3) 힌트: 섹션04(else if - 위에서부터 검사, 처음 참인 하나만 실행).
//     체를 겹쳐 놓을 때 구멍이 작은 체를 어디에 둬야 할까요?
console.log("===== 문제 2 =====");
const deliveryCount = 63;
// TODO: 여기에 작성

// ═══ 문제 3 ═══ 중첩 펼치기 — 왜 안 되는지 알려주기 [응용]
// (1) 요구사항: 아래 3중 중첩 코드는 결제가 실패해도 "아무것도 출력하지 않습니다".
//     사용자는 왜 결제가 안 되는지 알 수가 없습니다.
//
//       if (cardNumber) {
//         if (balance >= payAmount) {
//           if (isCardValid) {
//             console.log("결제 완료");
//           }
//         }
//       }
//
//     - 이것을 else if 로 펼쳐서, 걸린 이유를 사용자에게 알려주도록 고치세요.
//     - 검사 순서: 카드번호 없음 → 잔액 부족 → 카드 정지 → (전부 통과하면) 결제 완료
// (2) 기대 출력:
//     잔액이 부족합니다
// (3) 힌트: 섹션06(중첩 펼치기 - 잘못된 경우를 위에서 먼저 쳐내고, 정상 처리는 맨 아래 else에 한 번만)
console.log("===== 문제 3 =====");
const cardNumber = "1234-5678";
const balance = 12000;
const payAmount = 45000;
const isCardValid = true;
// TODO: 여기에 작성

// ═══ 문제 4 ═══ 입력값은 문자열이다 [응용]
// (1) 요구사항: 화면 입력창에서 받은 값은 숫자를 쳐도 항상 문자열로 들어옵니다.
//     아래 세 줄을 순서대로 출력하세요.
//     - inputAge 의 자료형을 typeof 로 출력
//     - inputAge 를 "그대로" 20과 비교한 결과 (>= 사용)
//     - Number() 로 바꾼 뒤 20 이상이면 "성인", 아니면 "미성년자" 출력 (삼항 연산자)
//     ⚠️ 두 번째 줄이 왜 그렇게 나오는지 설명할 수 있어야 합니다.
//        (>= 는 문자열을 숫자로 바꿔서 비교하지만, === 는 그렇지 않습니다)
// (2) 기대 출력:
//     string
//     true
//     성인
// (3) 힌트: 8일차 섹션11(typeof), 8일차 섹션12(형 변환), 섹션08(삼항 연산자)
console.log("===== 문제 4 =====");
const inputAge = "22";
// TODO: 여기에 작성

// ═══ 문제 5 ═══ [도전] switch 로 여러 값을 한 묶음으로
// (1) 요구사항: 요일 코드(1=월 … 7=일)를 받아 "평일" 또는 "주말"을 출력합니다.
//     - if 를 쓰지 말고 switch 로만 작성하세요.
//     - case 를 일곱 개 쓰되, 같은 결과인 것들은 break 없이 이어 붙여 한 묶음으로 만드세요.
//       (case 를 연달아 쓰면 아래 블록을 함께 씁니다 — 지금까지 "실수"로 배운 fall-through를
//        이번에는 의도적으로 이용하는 것입니다)
//     - 1~5 는 "평일", 6~7 은 "주말", 그 외는 "잘못된 요일 코드"
// (2) 기대 출력:
//     주말
// (3) 힌트: 섹션07(switch - case / break / default).
//     case 1: case 2: ... 처럼 case 만 연달아 쓰면 그 아래 블록을 공유합니다.
console.log("===== 문제 5 =====");
const dayCode = 7;
// TODO: 여기에 작성

// ═══ 문제 6 ═══ [도전] 결과 예측하기 — 조건문의 함정 모음
// (1) 요구사항: 아래 다섯 줄이 각각 무엇을 출력할지 먼저 종이에 적으세요.
//     그다음 주석을 풀어 실행하고, 예상과 다른 줄은 이유를 설명해 보세요.
//
//       if ("0") console.log("A"); else console.log("B");
//       if (0) console.log("C"); else console.log("D");
//       if ("") console.log("E"); else console.log("F");
//       console.log(5 > 3 > 1);
//       let n = 5;  if (n = 3) console.log("G");  console.log(n);
//
// (2) 기대 출력:
//     A
//     D
//     F
//     false
//     G
//     3
// (3) 힌트: 8일차 섹션08(참·거짓으로 취급되는 값 - "0"과 0은 다릅니다),
//     8일차 섹션15(비교 결과는 boolean이고, 그 boolean이 다시 비교에 쓰이면?),
//     섹션03(= 하나는 비교가 아니라 할당)
console.log("===== 문제 6 =====");
// TODO: 위 다섯 줄의 주석을 풀어 실행하고, 예상과 다른 줄의 이유를 적어 보세요.

// ═══ 문제 7 ═══ [도전] 종합 — 주문 객체로 배송비 계산
// (1) 요구사항: order 객체 하나만 보고 네 줄을 출력하세요. (반복문·함수 없이)
//     - 주문 상품 개수 (items 배열의 length)
//     - 마지막에 담은 상품 이름 ("길이 - 1" 인덱스 사용, [2] 라고 직접 쓰지 말 것)
//     - 총 주문 금액 (세 상품 가격의 합)
//     - 배송비 판정을 아래 규칙대로 한 줄로 출력
//         · 총액 50000 이상       → "무료배송"
//         · 총액 30000 이상       → "배송비 2500원"
//         · 제주·도서산간(isRemote가 true) → 위 결과에 상관없이 "추가 배송비 3000원"
//           (즉 먼저 검사해야 하는 조건이 무엇인지 판단해야 합니다)
//         · 그 외                 → "배송비 3500원"
// (2) 기대 출력:
//     주문 상품: 3개
//     마지막 담은 상품: 텀블러
//     총 주문 금액: 47000원
//     추가 배송비 3000원
// (3) 힌트: 8일차 섹션09·10(배열·객체 접근, 마지막 인덱스는 길이 - 1),
//     섹션04(else if 순서 - 어떤 조건을 맨 위에 둬야 할까요?)
console.log("===== 문제 7 =====");
const order = {
  customer: "김철수",
  isRemote: true,
  items: [
    { name: "머그컵", price: 12000 },
    { name: "노트", price: 5000 },
    { name: "텀블러", price: 30000 },
  ],
};
// TODO: 여기에 작성

// ═══ 문제 8 ═══ [도전] 카페 스탬프 적립 — 값이 계속 바뀌는 상황
// (1) 요구사항: 손님이 오늘 3잔을 주문했습니다. 아래 순서대로 여섯 줄을 출력하세요.
//     ① 오늘 주문 3잔을 스탬프에 더하고(복합 대입 연산자 사용) 현재 스탬프를 출력
//     ② 방문 횟수를 1 늘리고(증감 연산자 사용) 방문 횟수를 출력
//     ③ 스탬프가 10개 이상이면 "무료 음료 1잔 지급"을 출력하고 스탬프에서 10을 뺀다
//     ④ 남은 스탬프를 출력
//     ⑤ 방문 횟수가 5의 배수면 "단골 쿠폰 지급"을 출력 (나머지 연산자 사용)
//     ⑥ 회원 전화번호가 등록되어 있지 않으면 "전화번호 미등록", 있으면 "SMS 발송"
//        (삼항 연산자 사용. "등록되어 있지 않다"를 어떻게 검사할지 생각해 보세요)
//     ⚠️ stamp 와 visitCount 는 값이 바뀌므로 let, memberPhone 은 const 입니다.
//        왜 그렇게 선언되어 있는지도 설명할 수 있어야 합니다.
// (2) 기대 출력:
//     현재 스탬프: 11개
//     방문 횟수: 25회
//     무료 음료 1잔 지급
//     남은 스탬프: 1개
//     단골 쿠폰 지급
//     전화번호 미등록
// (3) 힌트: 8일차 섹션14(+= 와 ++), 8일차 섹션13(% 로 배수 판별),
//     8일차 섹션08(null - 개발자가 "비어 있다"고 표시한 값),
//     8일차 섹션11(null 확인은 typeof가 아니라 === null), 8일차 섹션06(템플릿 리터럴)
console.log("===== 문제 8 =====");
let stamp = 8;
let visitCount = 24;
const memberPhone = null;
// TODO: 여기에 작성

// 다 풀었다면 99_연습문제_심화1_조건문까지_정답.js 와 비교해 보세요.
