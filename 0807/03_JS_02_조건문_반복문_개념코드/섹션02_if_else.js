// 03_JS_02_조건문_반복문 개념코드 — 섹션 2. if / else (JavaScript)
// 읽는 순서: [기본] → [축약/다른 방법] → 실수 예시
// "실수:" 로 시작하는 줄은 일부러 틀리게 쓴 예입니다 (실행되지 않게 주석 처리).
// node 섹션02_if_else.js 로 실행하며 결과를 확인합니다.

// ── 섹션 2: if / else ──────────────
console.log("===== 섹션 2: if / else =====");
// 섹션 1의 if는 "조건이 맞으면 한다"였고, 거짓이면 그냥 지나갔습니다.
// else는 "아니면 대신 이걸 한다" — 갈림길에서 반드시 한쪽은 실행됩니다.
// [기본] 참이면 if 블록, 거짓이면 else 블록 - 두 블록이 동시에 실행되는 일은 없음
let stock = 3;
if (stock > 5) {         // 3 > 5 → false 이므로 이 블록은 건너뜀
  console.log("재고 충분");
} else {                 // 위가 거짓일 때만 여기로
  console.log("재고 부족 - 발주 필요");  // 출력: 재고 부족 - 발주 필요
}
// else에는 소괄호가 없다는 점에 주목하세요.
// "위 조건이 아닐 때"라는 뜻이 이미 정해져 있어서 따로 조건을 쓸 필요가 없습니다.
// 중괄호 { } 는 실행할 줄이 하나뿐이면 생략할 수 있지만, 우리는 항상 씁니다.
// 생략한 상태에서 나중에 한 줄을 더 추가하면 그 줄은 if와 상관없이 항상 실행되어
// 에러도 없이 조용히 망가집니다 — 찾기 가장 어려운 종류의 버그입니다.
// 실수: else (stock < 5) { ... }  // else는 조건을 쓰지 않음 -> 조건이 더 필요하면 else if
