// ============================================================
// 02단원 · 연습문제 — 표 설계하기
// ------------------------------------------------------------
// 실행: node 연습문제.js
// ============================================================
//
// 아래 `답.문제N = ...` 자리를 채우세요. 지금은 전부 null 입니다.
// 채운 뒤 실행하면 맞았는지 알려 줍니다.
//
//   ⬜ 아직 안 풀었습니다   →  null 그대로
//   ❌ 틀렸습니다          →  채웠는데 답이 아님
//   ✅ 정답
//
// ★ 순서대로 푸세요. 뒤로 갈수록 어렵습니다. 마지막 세 개는 [도전] 입니다.
// ★ 막히면 개념01~05 를 다시 보세요. 답은 연습문제_정답.js 에 있습니다.

import { PGlite } from "@electric-sql/pglite";

const db = await PGlite.create();

const 답 = {};


// ============================================================
// 문제 1 — 표 만들기
// ============================================================
//
// `작업자` 표를 만드는 CREATE TABLE 문을 문자열로 쓰세요.
//
//   작업자번호  정수
//   이름        글자
//   입사일      날짜
//
// (제약은 아직 안 겁니다. 칸 이름과 타입만 맞으면 됩니다)

답.문제1 = null; // TODO: `CREATE TABLE 작업자 (...)` 를 문자열로


// ============================================================
// 문제 2 — 타입 고르기
// ============================================================
//
// 각 값을 담기에 알맞은 Postgres 타입을 고르세요.
// 보기: INT · BIGINT · NUMERIC(12,2) · REAL · TEXT · BOOLEAN · DATE · TIMESTAMPTZ

답.문제2 = {
  설비단가: null,      // TODO: 원 단위 금액. 1원도 틀리면 안 됩니다
  설비온도: null,      // TODO: 센서가 읽은 섭씨 온도. 소수점 한 자리
  가동여부: null,      // TODO: 돌고 있나 아닌가
  점검기록시각: null,  // TODO: 언제 점검했는지. 해외 지사와 같이 봅니다
};


// ============================================================
// 문제 3 — 어느 타입이 정확한가
// ============================================================
//
// 0.1 을 1,000번 더했을 때 정확히 100 이 나오는 타입은?
// "NUMERIC" 또는 "REAL" 중 하나를 문자열로 쓰세요.

답.문제3 = null; // TODO:


// ============================================================
// 문제 4 — BIGINT 를 안전하게 꺼내기
// ============================================================
//
// `번호창고` 표에는 BIGINT 칸 `큰번호` 가 있습니다.
// 이 값을 `JSON.stringify` 해도 안 터지게 꺼내는 SELECT 문을 쓰세요.
//
//   · 결과 칸 이름은 `큰번호` 여야 합니다
//   · 값은 문자열로 나와야 합니다

답.문제4 = null; // TODO: `SELECT ... FROM 번호창고` 를 문자열로


// ============================================================
// 문제 5 — 날짜를 하루 안 밀리게 꺼내기
// ============================================================
//
// `일정` 표의 DATE 칸 `마감일` 을 자바스크립트에서 하루도 안 밀리게 받으려면
// 어떻게 꺼내야 하나요? SELECT 문을 쓰세요. 결과 칸 이름은 `마감일` 입니다.

답.문제5 = null; // TODO: `SELECT ... FROM 일정` 을 문자열로


// ============================================================
// 문제 6 — 표 고치기
// ============================================================
//
// 이미 만들어진 `설비목록` 표가 있습니다. 칸은 `번호`, `이름` 두 개입니다.
// 여기에 아래 두 가지를 하는 문장을 **한 문자열**로 쓰세요. (세미콜론으로 이으세요)
//
//   ① `도입일` 이라는 DATE 칸을 추가
//   ② `이름` 칸의 이름을 `설비명` 으로 변경

답.문제6 = null; // TODO: `ALTER TABLE ...; ALTER TABLE ...;`


// ============================================================
// 문제 7 — 제약 걸기
// ============================================================
//
// `점검` 표를 만드세요. 아래 규칙이 **표 정의만으로** 지켜져야 합니다.
//
//   점검번호  정수, 기본키
//   설비번호  정수, 비울 수 없음
//   결과      글자, 비울 수 없음, '정상' / '주의' / '불량' 중 하나만
//   비고      글자, 비워도 됨

답.문제7 = null; // TODO: `CREATE TABLE 점검 (...)` 을 문자열로


// ============================================================
// 문제 8 — 에러 코드
// ============================================================
//
// 각 상황에서 나오는 SQLSTATE 코드를 문자열로 쓰세요. (예: "42601")

답.문제8 = {
  낫널위반: null,   // TODO: NOT NULL 인 칸에 NULL 을 넣었을 때
  유니크위반: null, // TODO: UNIQUE 인 칸에 같은 값을 또 넣었을 때
  체크위반: null,   // TODO: CHECK 조건에 안 맞는 값을 넣었을 때
};


// ============================================================
// 문제 9 — DEFAULT
// ============================================================
//
// `주문` 표를 만드세요.
//
//   주문번호  정수, 기본키
//   상태      글자, 비울 수 없음, **안 적으면 '접수'** 가 들어가야 함
//   접수시각  시각, 비울 수 없음, **안 적으면 지금 시각**이 들어가야 함
//             ★ 해외에서도 같은 순간으로 보여야 합니다

답.문제9 = null; // TODO: `CREATE TABLE 주문 (...)` 을 문자열로


// ============================================================
// 문제 10 — 기본키를 자동으로
// ============================================================
//
// `알림` 표를 만드세요.
//
//   알림번호  **BIGINT**, 자동으로 매겨짐, 직접 값을 넣을 수 없음, 기본키
//   내용      글자, 비울 수 없음
//
// ★ SERIAL 말고 요즘 표준 문법을 쓰세요.

답.문제10 = null; // TODO: `CREATE TABLE 알림 (...)` 을 문자열로


// ============================================================
// 문제 11 — NULL 을 찾기
// ============================================================
//
// `설비` 표에서 **담당자가 정해지지 않은** 설비의 번호를 오름차순으로 뽑으세요.
// 결과 칸 이름은 `설비번호` 입니다.

답.문제11 = null; // TODO: `SELECT 설비번호 FROM 설비 WHERE ... ORDER BY 설비번호`


// ============================================================
// 문제 12 — ★ NOT IN 을 고치기
// ============================================================
//
// 아래 쿼리는 `정비중` 표에 NULL 이 한 줄 섞여 있어서 **0건**이 나옵니다.
//
//     SELECT 설비번호 FROM 설비
//     WHERE 설비번호 NOT IN (SELECT 설비번호 FROM 정비중)
//     ORDER BY 설비번호
//
// NULL 이 있어도 제대로 나오게 고쳐서 쓰세요. 결과 칸 이름은 `설비번호` 입니다.

답.문제12 = null; // TODO:


// ============================================================
// 문제 13 — [도전] 집계와 NULL
// ============================================================
//
// `생산` 표에는 `수량` 이 NULL 인 줄이 섞여 있습니다.
// **NULL 을 0으로 쳐서** 전체 줄 수로 나눈 평균을 구하세요.
// 결과 칸 이름은 `평균` 이고, 값은 정수(INT)여야 합니다.
//
// ★ 그냥 `avg(수량)` 을 쓰면 NULL 인 줄이 분모에서도 빠집니다.

답.문제13 = null; // TODO: `SELECT ... AS 평균 FROM 생산`


// ============================================================
// 문제 14 — [도전] 요구사항대로 표 설계하기
// ============================================================
//
// 아래 요구사항을 **전부** 표 정의에 담으세요. 애플리케이션 코드는 못 씁니다.
//
//   [작업지시] 표
//   · 지시번호는 BIGINT 로 자동으로 매겨지고, 직접 값을 넣을 수 없다. 기본키다
//   · 설비번호는 정수이고 반드시 있어야 한다
//   · 라인은 'A', 'B', 'C' 중 하나여야 하고 반드시 있어야 한다
//   · 지시일은 날짜이고, 안 적으면 오늘이 들어간다. 비울 수 없다
//   · 목표수량은 정수이고 0보다 커야 한다. 반드시 있어야 한다
//   · 완료수량은 정수이고 0 이상이어야 한다. 안 적으면 0 이 들어간다. 비울 수 없다
//   · 완료수량이 목표수량보다 클 수 없다   ← ★ 칸 두 개를 비교하는 CHECK
//   · 같은 설비에 같은 날짜의 지시가 두 번 있으면 안 된다  ← ★ 두 칸을 묶은 UNIQUE
//   · 상태 확인 제약의 **이름은 `수량_확인`** 으로 붙일 것 (완료수량 <= 목표수량 검사)

답.문제14 = null; // TODO: `CREATE TABLE 작업지시 (...)` 를 문자열로


// ============================================================
// 문제 15 — [도전] 더러운 데이터를 정리하고 제약 걸기
// ============================================================
//
// `옛날설비` 표에는 라인이 'A','B','C' 가 아닌 줄이 섞여 있습니다.
// 그 줄들의 라인을 **'A' 로 고친 뒤**, `라인_확인` 이라는 이름의 CHECK 제약을 거세요.
//
//   · 라인이 NULL 인 줄도 'A' 로 고칩니다
//   · 제약: 라인은 'A','B','C' 중 하나
//   · 한 문자열에 세미콜론으로 이어서 쓰세요
//   ★ 줄 수는 그대로여야 합니다. 지우면 안 됩니다

답.문제15 = null; // TODO: `UPDATE ...; ALTER TABLE ...;`


// ============================================================
// ↓↓↓ 아래는 채점 코드입니다. 고치지 마세요 ↓↓↓
// ============================================================

await db.exec(`
  CREATE TABLE 번호창고 (큰번호 BIGINT);
  INSERT INTO 번호창고 VALUES (9007199254740993);

  CREATE TABLE 일정 (마감일 DATE);
  INSERT INTO 일정 VALUES ('2026-03-15');

  CREATE TABLE 설비목록 (번호 INT, 이름 TEXT);

  CREATE TABLE 설비 (설비번호 INT PRIMARY KEY, 담당자 TEXT);
  INSERT INTO 설비 VALUES (1,'김반장'), (2,NULL), (3,'이반장'), (4,NULL);

  CREATE TABLE 정비중 (설비번호 INT);
  INSERT INTO 정비중 VALUES (1), (NULL);

  CREATE TABLE 생산 (설비번호 INT, 수량 INT);
  INSERT INTO 생산 VALUES (1,100), (2,NULL), (3,200), (4,NULL);

  CREATE TABLE 옛날설비 (번호 INT, 라인 TEXT);
  INSERT INTO 옛날설비 VALUES (1,'A'), (2,'999'), (3,NULL), (4,'B'), (5,'');
`);

let 맞은수 = 0;

async function 채점(번호, 확인) {
  const 답안 = 답[`문제${번호}`];

  const 안풀었나 =
    답안 === null ||
    답안 === undefined ||
    (typeof 답안 === "object" && Object.values(답안).every((값) => 값 === null));

  if (안풀었나) {
    console.log(`문제 ${번호} — ⬜ 아직 안 풀었습니다`);
    return;
  }

  try {
    const 통과 = await 확인(답안);
    if (통과) 맞은수 += 1;
    console.log(`문제 ${번호} — ${통과 ? "✅ 정답" : "❌ 틀렸습니다"}`);
  } catch (에러) {
    console.log(`문제 ${번호} — ❌ 에러 (${에러.code ?? 에러.name})`);
  }
}

// 답이 SQL 인 문제를 위해, 실행 뒤 되돌리는 도우미입니다.
async function 굴려보기(sql, 확인) {
  let 통과 = false;
  try {
    await db.transaction(async (tx) => {
      await tx.exec(sql);
      통과 = await 확인(tx);
      throw new Error("되돌리기");
    });
  } catch (에러) {
    if (에러.message !== "되돌리기") throw 에러;
  }
  return 통과;
}

async function 칸모양(tx, 표이름) {
  const 결과 = await tx.query(
    `SELECT column_name, data_type, is_nullable, column_default, is_identity
     FROM information_schema.columns WHERE table_name = $1 ORDER BY ordinal_position`,
    [표이름],
  );
  return 결과.rows;
}

// 성공하면 그 줄을 **그대로 둡니다.** 뒤에서 중복 검사를 해야 하기 때문입니다.
async function 막히나(tx, sql) {
  await tx.query(`SAVEPOINT 시험`);
  try {
    await tx.query(sql);
    await tx.query(`RELEASE SAVEPOINT 시험`);
    return false;
  } catch {
    await tx.query(`ROLLBACK TO SAVEPOINT 시험`);
    return true;
  }
}

console.log("===== 02단원 연습문제 채점 =====");

await 채점(1, (답안) =>
  굴려보기(답안, async (tx) => {
    const 칸 = await 칸모양(tx, "작업자");
    return (
      칸.length === 3 &&
      칸[0].column_name === "작업자번호" && 칸[0].data_type === "integer" &&
      칸[1].column_name === "이름" && ["text", "character varying"].includes(칸[1].data_type) &&
      칸[2].column_name === "입사일" && 칸[2].data_type === "date"
    );
  }),
);

await 채점(2, (답안) =>
  답안.설비단가 === "NUMERIC(12,2)" &&
  답안.설비온도 === "REAL" &&
  답안.가동여부 === "BOOLEAN" &&
  답안.점검기록시각 === "TIMESTAMPTZ",
);

await 채점(3, (답안) => 답안 === "NUMERIC");

await 채점(4, async (답안) => {
  const 결과 = await db.query(답안);
  const 값 = 결과.rows[0].큰번호;
  return typeof 값 === "string" && 값 === "9007199254740993" && JSON.stringify(결과.rows).length > 0;
});

await 채점(5, async (답안) => {
  const 값 = (await db.query(답안)).rows[0].마감일;
  return 값 === "2026-03-15";
});

await 채점(6, (답안) =>
  굴려보기(답안, async (tx) => {
    const 칸 = await 칸모양(tx, "설비목록");
    const 이름들 = 칸.map((하나) => 하나.column_name);
    return (
      이름들.includes("설비명") && !이름들.includes("이름") &&
      칸.some((하나) => 하나.column_name === "도입일" && 하나.data_type === "date")
    );
  }),
);

await 채점(7, (답안) =>
  굴려보기(답안, async (tx) => {
    const 정상 = !(await 막히나(tx, `INSERT INTO 점검 VALUES (1, 10, '정상', NULL)`));
    const 결과오타 = await 막히나(tx, `INSERT INTO 점검 VALUES (2, 10, '가둥', NULL)`);
    const 설비널 = await 막히나(tx, `INSERT INTO 점검 VALUES (3, NULL, '정상', NULL)`);
    const 번호중복 = await 막히나(tx, `INSERT INTO 점검 VALUES (1, 11, '주의', NULL)`);
    return 정상 && 결과오타 && 설비널 && 번호중복;
  }),
);

await 채점(8, (답안) =>
  답안.낫널위반 === "23502" && 답안.유니크위반 === "23505" && 답안.체크위반 === "23514",
);

await 채점(9, (답안) =>
  굴려보기(답안, async (tx) => {
    await tx.query(`INSERT INTO 주문 (주문번호) VALUES (1)`);
    const 줄 = (await tx.query(`SELECT 상태, 접수시각 FROM 주문 WHERE 주문번호 = 1`)).rows[0];
    const 칸 = await 칸모양(tx, "주문");
    const 시각칸 = 칸.find((하나) => 하나.column_name === "접수시각");
    return (
      줄.상태 === "접수" &&
      줄.접수시각 instanceof Date &&
      시각칸.data_type === "timestamp with time zone" &&
      시각칸.is_nullable === "NO"
    );
  }),
);

await 채점(10, (답안) =>
  굴려보기(답안, async (tx) => {
    const 칸 = await 칸모양(tx, "알림");
    const 번호칸 = 칸.find((하나) => 하나.column_name === "알림번호");
    if (!번호칸 || 번호칸.data_type !== "bigint" || 번호칸.is_identity !== "YES") return false;
    await tx.query(`INSERT INTO 알림 (내용) VALUES ('첫 알림')`);
    const 자동 = (await tx.query(`SELECT 알림번호 FROM 알림`)).rows[0].알림번호;
    const 직접막힘 = await 막히나(tx, `INSERT INTO 알림 VALUES (99, '몰래')`);
    return Number(자동) === 1 && 직접막힘;
  }),
);

await 채점(11, async (답안) => {
  const 줄들 = (await db.query(답안)).rows.map((줄) => 줄.설비번호);
  return JSON.stringify(줄들) === JSON.stringify([2, 4]);
});

await 채점(12, async (답안) => {
  const 줄들 = (await db.query(답안)).rows.map((줄) => 줄.설비번호);
  return JSON.stringify(줄들) === JSON.stringify([2, 3, 4]);
});

await 채점(13, async (답안) => {
  const 값 = (await db.query(답안)).rows[0].평균;
  return Number(값) === 75;
});

await 채점(14, (답안) =>
  굴려보기(답안, async (tx) => {
    const 칸 = await 칸모양(tx, "작업지시");
    const 지시번호 = 칸.find((하나) => 하나.column_name === "지시번호");
    if (!지시번호 || 지시번호.data_type !== "bigint" || 지시번호.is_identity !== "YES") return false;


    const 정상 = !(await 막히나(tx, `INSERT INTO 작업지시 (설비번호,라인,목표수량) VALUES (1,'A',100)`));
    const 라인틀림 = await 막히나(tx, `INSERT INTO 작업지시 (설비번호,라인,목표수량) VALUES (2,'999',100)`);
    const 목표영 = await 막히나(tx, `INSERT INTO 작업지시 (설비번호,라인,목표수량) VALUES (3,'A',0)`);
    const 완료초과 = await 막히나(tx, `INSERT INTO 작업지시 (설비번호,라인,목표수량,완료수량) VALUES (4,'B',10,11)`);
    const 중복 = await 막히나(tx, `INSERT INTO 작업지시 (설비번호,라인,목표수량) VALUES (1,'A',50)`);

    const 첫줄 = (await tx.query(`SELECT 완료수량, 지시일 FROM 작업지시 WHERE 설비번호 = 1`)).rows[0];
    const 기본값 = 첫줄 && 첫줄.완료수량 === 0 && 첫줄.지시일 instanceof Date;

    const 이름붙임 = (await tx.query(
      `SELECT count(*)::int AS 개수 FROM pg_constraint
       WHERE conrelid = '작업지시'::regclass AND conname = '수량_확인'`,
    )).rows[0].개수 === 1;

    return 정상 && 라인틀림 && 목표영 && 완료초과 && 중복 && 기본값 && 이름붙임;
  }),
);

await 채점(15, (답안) =>
  굴려보기(답안, async (tx) => {
    const 줄수 = (await tx.query(`SELECT count(*)::int AS 개수 FROM 옛날설비`)).rows[0].개수;
    const 나쁜것 = (await tx.query(
      `SELECT count(*)::int AS 개수 FROM 옛날설비 WHERE 라인 IS NULL OR 라인 NOT IN ('A','B','C')`,
    )).rows[0].개수;
    const 제약있나 = (await tx.query(
      `SELECT count(*)::int AS 개수 FROM pg_constraint
       WHERE conrelid = '옛날설비'::regclass AND conname = '라인_확인'`,
    )).rows[0].개수 === 1;
    const 막히나결과 = await 막히나(tx, `INSERT INTO 옛날설비 VALUES (9, 'Z')`);
    return 줄수 === 5 && 나쁜것 === 0 && 제약있나 && 막히나결과;
  }),
);

console.log(`===== 15문제 중 ${맞은수}개 정답 =====`);
// 출력: ===== 02단원 연습문제 채점 =====
// 출력: 문제 1 — ⬜ 아직 안 풀었습니다
// 출력: 문제 2 — ⬜ 아직 안 풀었습니다
// 출력: 문제 3 — ⬜ 아직 안 풀었습니다
// 출력: 문제 4 — ⬜ 아직 안 풀었습니다
// 출력: 문제 5 — ⬜ 아직 안 풀었습니다
// 출력: 문제 6 — ⬜ 아직 안 풀었습니다
// 출력: 문제 7 — ⬜ 아직 안 풀었습니다
// 출력: 문제 8 — ⬜ 아직 안 풀었습니다
// 출력: 문제 9 — ⬜ 아직 안 풀었습니다
// 출력: 문제 10 — ⬜ 아직 안 풀었습니다
// 출력: 문제 11 — ⬜ 아직 안 풀었습니다
// 출력: 문제 12 — ⬜ 아직 안 풀었습니다
// 출력: 문제 13 — ⬜ 아직 안 풀었습니다
// 출력: 문제 14 — ⬜ 아직 안 풀었습니다
// 출력: 문제 15 — ⬜ 아직 안 풀었습니다
// 출력: ===== 15문제 중 0개 정답 =====

// ★ 위 `// 출력:` 은 **아무것도 안 푼 상태**의 결과입니다.
//   문제를 풀면 ✅ 로 바뀌면서 이 주석과 달라집니다. 정상입니다.

await db.close();
